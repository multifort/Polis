# Polis 研发任务清单（WBS · V1）

> 由 `docs/design/02~07` 的任务分解汇总而成，按里程碑组织；与 [研发计划.md](研发计划.md) 配套。
> 列：ID / 任务 / 设计章节 / 依赖 / 验收标准 / 估时(人天,初估) / 轨道(BE后端·FE前端·INF基础设施)。
> 纪律：每条进开发前过 **DoR**、完成过 **DoD**（见 `CLAUDE.md §3`、`docs/constraints/15`）。
> 估时为初估，进入里程碑前按 DoR 复评；>3 天的须拆分。

---

## M0 脚手架（仓库可开发、门禁就位）

| ID | 任务 | 设计 | 依赖 | 验收标准 | 估时 | 轨 |
|---|---|---|---|---|---|---|
| T0.1 | 后端脚手架：FastAPI modular monolith 骨架（org/planner/runtime/memory/model/observability/api 模块占位）+ uv 锁定 + Makefile + 分层目录 | 01,07§2 | — | `make dev` 起 uvicorn，`/health` 通；分层骨架就位 | 2 | BE |
| T0.2 | 质量门禁：pre-commit/pre-push（ruff+mypy+pytest+gitleaks+bandit+commitlint+pip-audit） | 14 E11/E14 | T0.1 | 提交触发全套检查，全绿才过；`--no-verify` 需说明 | 1.5 | INF |
| T0.3 | docker-compose 起栈：postgres(pgvector)/temporal/litellm/langfuse + `.env.example`（= 07 T7.2） | 07§3 | — | 一键起 4 基础设施 + app；compose up 健康 | 2 | INF |
| T0.4 | Alembic 基线 + 本地集成脚本（testcontainers）+ 里程碑看板初始化(T7.5) | 14 E9,07§5 | T0.1,T0.3 | `alembic upgrade head` 跑空基线；CI 本地脚本可跑测试 | 1.5 | INF |

## M1 数据底座（建库 + 全量 DDL + 隔离骨架）

| ID | 任务 | 设计 | 依赖 | 验收标准 | 估时 | 轨 |
|---|---|---|---|---|---|---|
| T2.1 | DDL+迁移：org/org_member/role/agent/agent_version/agent_capability/scenario_preset | 02§3 | T0.4 | 迁移可执行，外键/索引/hnsw 生效 | 2 | BE |
| T5.1 | DDL：memory(+vector hnsw)/result_envelope/artifact_descriptor | 05§2 | T0.4 | 向量检索可用，索引生效 | 1.5 | BE |
| T4.6 | DDL：skill/skill_version/skill_invocation | 04§6 | T0.4 | 调用可计费/可观测字段就位 | 1 | BE |
| T6.2 | DDL：model_catalog + cost_aware_pick 骨架 | 06§1.2 | T0.4 | 够用选便宜的选择器可单测 | 1.5 | BE |
| T3.3 | 能力词表：capability 表(+embedding hnsw) + 受控新增流程 + seed 采购域词表 | 03§7.3 | T0.4 | Planner/Agent/Router 共用同一 `capability.key` | 2 | BE |
| T7.1 | 全局迁移聚合 + seed（基础词表/采购预设/模型目录） | 07§5 | 上列 DDL | 一键建库、基础数据/预设就位 | 1.5 | INF |
| T8.1 | 多租户隔离骨架 + audit_log：统一 org 过滤层 + 审计写入（= 07 T7.3 骨架） | 07§1.1 | T2.1 | 数据访问统一经隔离层；操作留痕 | 2 | BE |
| T9.1 | app_user/auth_session DDL + 注册/登录/JWT(access+refresh) + argon2 | 09§2,§3 | T0.4 | 登录拿到 token；刷新/登出/改密吊销生效 | 3 | BE |
| T9.4 | PostgreSQL RLS 策略 + 每请求 SET LOCAL app.current_org（与 T8.1 组成双保险） | 09§5 | T8.1 | 漏写 org 过滤仍被 RLS 拦 | 2 | BE |

## M2 立邦闭环（意图 → 建出可用「公司」）

| ID | 任务 | 设计 | 依赖 | 验收标准 | 估时 | 轨 |
|---|---|---|---|---|---|---|
| T2.2 | Agent/Preset 配置 schema + 校验器（Pydantic/JSON Schema） | 02§4 | T2.1 | 非法配置被拒并给字段级报错 | 2 | BE |
| T2.3 | IntentParser（onboarding 问答→结构化意图，LLM） | 02§5 | T6.1 | 10 个样例意图人评准确率 ≥80% | 2 | BE |
| T2.4 | PresetMatcher（pgvector 检索最匹配预设） | 02§5 | T2.1,T6.1 | 命中 Top1 在人评相关集 ≥80% | 1.5 | BE |
| T2.5 | Assembler + OrgInstantiator（建 Org + 实例化 Agent + 记忆命名空间） | 02§5 | T2.2,T2.4 | 预设场景一键建出可用花名册 | 2.5 | BE |
| T2.6 | GapGenerator + AgentValidator（缺口生成 + 样例/断言/judge） | 02§5,06§4 | T2.5,T6.1 | 生成 Agent 默认 draft，验证不过不上线 | 3 | BE |
| T2.7 | Provision/Org/Agent REST API（含 publish/rollback） | 02§6 | T2.5,T6.7 | 端到端 provision 通，人审后 active | 2 | BE |
| T2.8 | 编配端到端联调（采购场景） | 02§9 | T2.3–T2.7 | 问答→建出「采购公司」<2min，可进 03 | 2 | BE |
| T9.2 | 当前城邦中间件 + 多 Org 切换（X-Org-Id 校验成员）+ /api/me | 09§4 | T9.1,T2.1 | 非成员 403；切 Org 无需重登 | 2 | BE |
| T9.3 | 权限矩阵 + 路由守卫 require_role（owner/approver/member） | 09§6 | T9.2 | member 不能审批/管成员，owner 可 | 2 | BE |

## M3 大脑（目标 → DAG → 编排）

| ID | 任务 | 设计 | 依赖 | 验收标准 | 估时 | 轨 |
|---|---|---|---|---|---|---|
| T3.1 | Plan schema + 校验器 validate()（无环/可达/能力可满足/预算/危险 gate） | 03§3,§4 | T3.3 | 注入坏 DAG 全部被拦 | 2.5 | BE |
| T3.2 | Planner 五级流水线（模板优先 + 全自动拆解兜底） | 03§2 | T3.1 | 模板命中可填槽产出合法 DAG | 3 | BE |
| T3.4 | Router 打分 + 冷启动公式（select()） | 03§7 | T3.3,T2.1 | 冷启动用降权式；有历史后切完整式 | 2.5 | BE |
| T3.5 | Temporal Workflow/Activity（TaskWorkflow + run_node，串/并/人审 signal/retry） | 03§5 | T3.1,T0.3 | 串/并/人审/重试全通 | 3 | BE |
| T3.6 | 有界动态重规划 _bounded_replan（局部重排，过校验，有上限） | 03§6 | T3.5 | 失败触发局部重排且过校验、有上限 | 2 | BE |

## M4 执行（单节点经 Agent + 工具跑通）

| ID | 任务 | 设计 | 依赖 | 验收标准 | 估时 | 轨 |
|---|---|---|---|---|---|---|
| T4.1 | ContextAssembler（注入三样：记忆切片+技能+模型/短时凭证） | 04§1 | T5.3,T6.1,T6.3 | 三样齐全、凭证短时、用完即弃 | 2 | BE |
| T4.2 | SkillLoader 双形态（手册注入 + 工具→MCP，最小权限过滤） | 04§2 | T4.6 | 越权工具被过滤 | 2 | BE |
| T4.3 | lite-agent 执行循环 _loop（多轮工具调用 + 超步保护） | 04§3 | T4.1,T4.2 | 多轮工具调用 + 超步标 replannable | 2.5 | BE |
| T4.4 | MCP Runtime/Registry（McpClient + 接 browser-pilot 等） | 04§4 | T4.2 | 调通 ≥2 个 MCP server | 2 | BE |
| T4.5 | Guardrails 三道防线（输入/回流过滤 + 权限 + 危险 gate） | 04§5 | T4.3,T6.7 | 注入样例被拦并审计 | 2.5 | BE |
| T4.7 | 与 03/05/06 联调（端到端单节点执行 + 出处入库） | 04§9 | M3,M5,M6 | 采购单节点跑通，出处入库 | 2 | BE |

## M5 记忆（写入/检索/出处/凭证短时）

| ID | 任务 | 设计 | 依赖 | 验收标准 | 估时 | 轨 |
|---|---|---|---|---|---|---|
| T5.2 | 写入管线 write()（抽取/评分/过滤去噪去重 + 出处入库） | 05§3 | T5.1,T6.1 | 噪声/重复被过滤，出处入库 | 2 | BE |
| T5.3 | 检索管线 retrieve()（RAG + rerank + 作用域权限过滤，返回摘要） | 05§4 | T5.1,T6.1 | 跨作用域权限正确，返回摘要 | 2.5 | BE |
| T5.4 | 衰减/遗忘定时任务 decay_job | 05§5 | T5.1 | 低价值/过期被清理 | 1 | BE |
| T5.5 | 共享记忆并发裁决 upsert_shared_fact（高置信覆盖/冲突标记） | 05§6 | T5.2 | 高置信覆盖、冲突标记不静默合并 | 1.5 | BE |
| T5.6 | 记忆治理（行级隔离 + 读写审计） | 05§9 | T8.1 | A 不能读 B 的 org 记忆 | 1.5 | BE |
| T5.7 | 与 04 联调（执行→写回→再检索） | 05§11 | T5.2,T5.3 | 第二个 Agent 读到第一个写的带出处事实 | 1.5 | BE |
| T6.3 | Credential Broker（信封加密 + 任务级短时句柄 + 审计） | 06§2 | T6.2,T8.1 | 共享池下跨用户不串 Key；用完即焚 | 3 | BE |

## M6 治理与观测（网关/观测/评测/审批）

| ID | 任务 | 设计 | 依赖 | 验收标准 | 估时 | 轨 |
|---|---|---|---|---|---|---|
| T6.1 | LiteLLM 网关封装 ModelGateway（resolve/chat/embed） | 06§1 | T0.3 | ≥3 家模型可调，成本统计可见 | 2 | BE |
| T6.4 | Langfuse 接线（trace 上报 + trace_ref） | 06§3 | T6.1 | 一次任务全链路可回放 | 1.5 | BE |
| T6.5 | Evaluator（断言 + LLM-judge + 回归集） | 06§4 | T6.1 | 改动前后回归可对比 | 2.5 | BE |
| T6.6 | Run Manifest（落库 + 复现） | 06§5 | T2.1,T3.5 | 任务可凭 manifest 复现 | 1 | BE |
| T6.7 | 审批收件箱 approval（表 + API + Temporal signal 回写） | 06§6 | T3.5 | 四类人审统一队列，决定回写 Workflow | 2.5 | BE |

## M7 端到端验收门（采购场景 + 量化）

| ID | 任务 | 设计 | 依赖 | 验收标准 | 估时 | 轨 |
|---|---|---|---|---|---|---|
| T8.2 | 采购场景端到端联调（意图→编配→规划→执行→审批→记忆→观测） | 07§4 | M2–M6 | 端到端跑通一个真实目标 | 3 | BE |
| T3.7 | eval 联调 + 验收门量化（10~20 目标，量四项指标） | 03§9,06§4 | T8.2,T6.5 | 跑出 DAG 可用率/路由命中/人审通过/成本时延 | 2.5 | BE |
| T8.3 | org_id 隔离回归测试集（A/B 两租户互不可见） | 14 E17 | T8.1 | 隔离用例纳入门禁、全绿 | 1.5 | BE |

## 前端轨（MW1/MW2，与后端并行）

| ID | 任务 | 设计 | 依赖 | 验收标准 | 估时 | 轨 |
|---|---|---|---|---|---|---|
| TW.1 | 前端脚手架：Next.js + 设计令牌(10/10a)接入 + OpenAPI 类型生成 + 鉴权骨架 | 11,10a,07§2 | T0.1 | 令牌生效；类型从 OpenAPI 生成；登录骨架通 | 3 | FE |
| TW.2 | 聊天界面（接 chat API，SSE 流式 + 四态:加载/空/错误/成功） | 11,10 A10 | TW.1 | 流式对话可用，四态齐全 | 3 | FE |
| TW.3 | Org 管理 + 编配向导（onboarding 问答→provision→待审花名册） | 02§6,10 | TW.1,T2.7 | 走完问答建出公司、展示待审 Agent | 3 | FE |
| TW.4 | 计划审批界面（DAG 预览/编辑 + 预估成本 + 通过/驳回） | 03,06§7 | TW.1,T6.7 | 可视化 DAG、确认触发 Temporal | 3 | FE |
| TW.5 | 公司看板（任务状态/当前角色/已花成本/卡点） | 10 A13 | TW.1,T6.6 | 看板四类卡片实时反映任务 | 2.5 | FE |
| TW.6 | 审批收件箱（统一「待我处理」队列 + 决定回写） | 06§6,10 | TW.1,T6.7 | 四类人审统一列表，决定生效 | 2 | FE |

## 横切/后置

| ID | 任务 | 说明 | 状态 |
|---|---|---|---|
| T7.3 | 多租户隔离 + 审计 | 骨架在 T8.1，回归在 T8.3，贯穿全程 | 进行(随功能) |
| T7.5 | 里程碑跟踪看板 | M1~M7 状态流转，每里程碑有验收项 | M0 起 |
| T7.4 | CI 流水线 | E4 初期排除：先本地 pre-commit/pre-push，引 CI 后迁移 | **后置** |
| T9.5 | 成员邀请流程 | org_invite + 接受：邀请→成员生效，幂等/过期正确 | V2(或 M2 精简) |

---

## 估时汇总（初估，人天）

| 里程碑 | 合计 |
|---|---|
| M0 | 7 |
| M1 | 16.5（含身份 T9.1/T9.4） |
| M2 | 21（含身份 T9.2/T9.3） |
| M3 | 13 |
| M4 | 13 |
| M5 | 13 |
| M6 | 9.5 |
| M7 | 7.5 |
| 前端 MW1+MW2 | 16.5 |
| **后端关键路径(M0→M7)合计** | **约 100.5**（含身份层 T9.1–T9.4） |

> 单人串行约 18~20 周；2~3 人按 §3 并行轨可压到约 9~12 周（取决于 M5/M6/前端并行度与评测迭代次数）。
> 一切以 V1 验收门为准：未达门**回炉调参不加模块**（F3/F9）。

---

# V2 任务清单（WBS · 能协同）

> 由 `docs/design/v2/`（00 总纲 + 01–05 + ADR-0009~0014）分解而成，按阶段组织；与 [研发计划.md](研发计划.md) §9 配套。
> 列同上：ID / 任务 / 设计 / 依赖 / 验收标准 / 估时(人天,初估) / 轨。每条进开发前过 DoR，>3 天再拆。

## 阶段 0 · 地基

| ID | 任务 | 设计 | 依赖 | 验收标准 | 估时 | 轨 |
|---|---|---|---|---|---|---|
| R1a | skill/plan_template 加 `visibility`/`embedding`/hnsw + 内置资产置 public + embedding 回填 | v2/04 §3,§14 | M6 embed | 三层资产可语义检索；内置资产 public | 3 | BE |
| R1b | **RLS 读放行 public**（读=org∪public、写仍严格 org）+ 扩 T8.3 隔离回归 | v2/04 §5,§14.5 | R1a,RLS | org 只见 私有∪public，绝不见他 org 私有；回归全绿 | 2 | BE |
| B1 | 任务黑板：`result_envelope.content` + `read_node_output`/`query_task_memory` 内置工具 + 依赖摘要注入 | v2/02 §7,§16 | M4/M5 | 下游确定性按 id 拿到上游产出(协同可靠性100%) | 3 | BE |
| P1a | `task` 表 + `task_run.task_id` + 运行/重跑 API（1 任务:N 执行记录） | v2/05 §4,§17 | — | 任务可保存、可多次运行、留多条记录 | 3 | BE |
| P1b | 任务列表/详情前端（可复用、重跑） | v2/05 §6 | P1a | 列表+重跑可用 | 2 | FE |
| S1 | 节点状态机（needs_rework/replanning）+ 关键节点 Evaluator 质量检测 | v2/03 §3,§4,§15 | Evaluator,B1 | 状态转移受控；关键节点 judge<τ 可触发 | 3 | BE |

## 阶段 A · 生成式内核

| ID | 任务 | 设计 | 依赖 | 验收标准 | 估时 | 轨 |
|---|---|---|---|---|---|---|
| A1 | 模板/角色/技能/能力**语义检索**（替换关键词/遍历）`retrieve_template` | v2/01 §4.2,§14.2 | R1a | 命中相似且能力可满足的模板；阈值退化正确 | 3 | BE |
| A2a | RAG 接地 DAG 生成（prompt + exemplars + PlanDag 强校验） | v2/01 §4.3,§14.3 | A1 | 未命中→生成合法 PlanDag | 3 | BE |
| A2b | 双校验 + 有界自修复 + 错误反馈回路（偿还 **TD-020**） | v2/01 §14.3 | A2a | 生成 DAG 结构合法率≥80%（含 1 次自修复） | 3 | BE |
| R2 | `role_template` 表 + preset 改引用 + 编配从 role_template 选/拼 | v2/04 §3,§13 | R1a | 角色模板可检索/实例化；preset 引用生效 | 3 | BE |
| A3a | `compose_agent`：拼已审 Skill 成 Agent（自动）+ eval 激活 | v2/01 §5,§14.5 | A1,B1,R2 | 节点缺 Agent 时自动拼装并通过 eval | 3 | BE |
| A3b | 缺 Skill 生成草稿 + 沙箱试跑 + 人审入口 | v2/01 §5.4 | A3a | 缺 Skill 走草稿→沙箱→人审→激活闭环 | 3 | BE/FE |
| B2 | 分层检索（org→规划 / role+org→Agent）+ 预算截断 | v2/02 §6,§16.3 | B1,A1 | 各消费者读对层；上下文 ≤ B_node | 3 | BE |
| 门 | 扩 M7 目标集到"模板外/新维度"跑：无模板 DAG 可用率≥70% / 合法率≥80% / 编配覆盖率≥90% | v2/00 §4 | 上列 | 达门进阶段 B | — | — |

## 阶段 B · 稳态与质量

| ID | 任务 | 设计 | 依赖 | 验收标准 | 估时 | 轨 |
|---|---|---|---|---|---|---|
| S2a | 分级纠错（retry→rework→escalate→abort）+ 纠错预算计数 | v2/03 §4,§15.3 | S1,A2b | 软/硬失败按级纠错；超界转人审/abort | 3 | BE |
| S2b | 局部重规划（子图圈定→回调内核→重 validate→拼接）+ 不变量 | v2/03 §14.2,§15.4 | S2a | 最小作用域重规划；每次改图过 validate+不变量 | 3 | BE |
| A4 | Evaluator 三件套常态化 + 热路径阈值闸 + 沉淀回写 | v2/01 §6 | A2b/A3a | judge≥τ 放行；不达标触发纠错 | 3 | BE |
| B3 | task→role→org **自动晋升**（阈值+去重+org并发裁决）+ 分层 decay | v2/02 §5,§16.4 | B1/B2 | 晋升精确率≥90%（人抽检正确且有出处） | 3 | BE |
| B4 | 上下文 token 预算 enforce + token 观测 + 调参 | v2/02 §8,§16.5 | B2 | 节点上下文预算达标率≥95% | 2 | BE |
| S3 | org 并发上限/公平调度 + 预算**提示**（不阻断） | v2/03 §6,§14.3 | S1 | 超并发排队；超预算告警不阻断 | 3 | BE |
| S4 | workflow versioning + heartbeat + durable human gate 加固 | v2/03 §7,§15.5 | S1 | worker 重启/版本变更后在飞任务正确续跑 | 3 | BE |
| 门 | 自动纠错成功率≥60% / 任务完成率≥90% / 长任务恢复100% / 并发无串扰100% | v2/00 §4 | 上列 | 达门进阶段 C | — | — |

## 阶段 C · 产品化与飞轮

| ID | 任务 | 设计 | 依赖 | 验收标准 | 估时 | 轨 |
|---|---|---|---|---|---|---|
| P2a | MinIO 接入（docker-compose + 客户端 + `{org_id}/{task_id}/` 前缀隔离） | v2/05 §9,§17 | — | 对象存储可用，按 org 隔离 | 2 | INF/BE |
| P2b | 附件 `input_schema` + 上传 → artifact → 黑板按需注入 | v2/05 §5,§16.2 | P2a,B1 | 上传附件被任务正确读取并影响产出 | 3 | BE |
| P3a | 执行记录列表 + 详情（复用观测页 M6-H-4） | v2/05 §6 | P1a | 任意任务可查全部历次执行记录 | 2 | FE |
| P3b | 结果产物 + md/pdf 导出 + 下载 | v2/05 §7,§17.4 | P2a | 结果一键导出 md/pdf 并下载 | 3 | BE |
| P4 | 看板（跨任务/场景运营统计 + 实时过程观测） | v2/05 §8,§16.4 | P3,S3 | 成本/成功率/时延统计与明细一致 | 3 | BE/FE |
| R3 | 场景模板沉淀（满意"存为模板" + embedding 回写 + 大类/小类） | v2/04 §6,§13.2 | R1,A4,P3 | 满意任务沉淀为可检索场景模板 | 3 | BE/FE |
| R4 | 生成 Agent 经 eval → `role_template` 自动沉淀（source=generated） | v2/04 §6 | R2,A3a | 自动沉淀的角色模板被后续任务复用≥60% | 2 | BE |
| P5 | 场景库树导航（大类 > 小类 > 具体场景） | v2/05 §3,§4 | R3 | 场景按树归类、不平铺 | 2 | FE |
| 门 | 历史可追溯100% / 附件·导出端到端通 / 复用命中率↑ / 自动沉淀有效性≥60% | v2/00 §4 | 上列 | V2 整体达门 | — | — |

## V2 估时汇总（初估，人天）
| 阶段 | 合计 |
|---|---|
| 0 地基 | 16（R1a3 R1b2 B1 3 P1a3 P1b2 S1 3） |
| A 生成内核 | 21（A1 3 A2a3 A2b3 R2 3 A3a3 A3b3 B2 3） |
| B 稳态质量 | 20（S2a3 S2b3 A4 3 B3 3 B4 2 S3 3 S4 3） |
| C 产品飞轮 | 20（P2a2 P2b3 P3a2 P3b3 P4 3 R3 3 R4 2 P5 2） |
| **V2 合计** | **约 77 人天** |

> 单人串行约 15~16 周；2~3 人按 §9.3 并行轨（记忆/产品/协同三轨与内核轨并行）可压到约 7~9 周。
> 关键路径 `R1→A1→A2→S2→A4→R3`；**每阶段过数据门才进下一阶段，未达门回炉调参不加模块**（F3/F9）。

---

## V2 进度快照 + 后续优先级（截至 2026-07-04）

### 已完成（合并 main）
- **阶段 0** ✅ R1 / B1 / P1(含 P1b 前端) / S1。
- **阶段 A** ✅ A1(检索 + 校准 命中率 0→100%) / A2(RAG 生成，门 100%/90%) / A3a(拼装+背书) / A4(自动背书，TD-033 升试产出硬门控) / B2(分层接地，飞轮闭环) / **Skill 风险分级放行**(manual 自动 eval / tool 人审，ADR-0009 修订)。
- **阶段 B** ✅ S2a(rework+escalate) / S3(并发闸+预算提示) / S4(heartbeat + ADR-0012) / B3(记忆自动晋升飞轮) / B4(分层可配预算)。**B 稳态门已回炉清掉**（任务完成率 25%→100%，judge 加 rubric 把方差 ±0.5→±0.06；自动纠错/并发无串扰/长任务恢复机制均达标）。
- **C0 交互重构一期** ✅ 应用壳、工作台首屏、工作列表、工作详情、看板入口、花名册与设置页均进 AppShell，新 IA 主路径可走通。
- **阶段 C 产品化** ✅ P2(附件+MinIO) / P3(执行记录+md/pdf 导出) / P4(看板) / **R3(场景模板沉淀)** / **P5(场景库树导航)** / **R4(生成 Agent → role_template 自动沉淀机制)**。R3 已落：`plan_template.domain/subcategory/acceptance_criteria/source`、`POST /api/plans/{id}/save-as-template`、`GET /api/catalog/templates`、前端「存为模板」入口；R3 加固已补即时 embedding 回写与回归测试。R4 已落：新增 `role_template` 资产表，`compose_agent` 在生成 Agent 通过 eval 后沉淀私有角色模板，并纳入 embedding backfill。
- **偿还 TD**：017 预设语义 / 023 调用计量 / 011 登录失败审计 / 030 能力语义去重原语。

### 后续任务（按优先级 + 依赖；映射上表 WBS ID）
| 优先 | 任务（WBS ID） | 依赖/前置 | 备注 |
|---|---|---|---|
| P0 | ~~**交互重构·一期**（C0）~~ | 用户定档期 | ✅ 已完成 2026-07-01；工作台/工作列表/工作详情/看板/花名册主路径已按新 IA 接入 |
| P1 | ~~**P2** 附件 + MinIO（P2a/P2b）~~ | MinIO | ✅ 已完成 2026-07-01（T7.6 + P2b-1/P2b-2，见下方任务卡）；补齐「无上传入口」+「附件影响产出」 |
| P1 | ~~**P3** 执行记录 + 导出（P3a/P3b）~~ | P1a✅ | ✅ 已完成 2026-07-01（见下方任务卡）；P3a 复用观测页 + 执行记录弹窗；P3b md/pdf 导出（reportlab 内嵌中文字体） |
| P2 | ~~**P4** 看板~~ | P3✅,S3✅ | ✅ 已完成 2026-07-02（见下方任务卡）；复用 observability 聚合 + 新增跨任务统计 |
| P2 | ~~**R3 场景模板沉淀**~~ | A4✅/R1✅/P3✅ | ✅ 已完成 2026-07-03；可将满意计划存为私有模板，场景库 API 已按 `domain` 支持筛选 |
| P2 | ~~**P5 场景库树导航**~~ | R3✅ | ✅ 已完成；前端按「大类 > 小类 > 具体场景」树形浏览可见模板，基于 `domain`/`subcategory` |
| P2 | ~~**R4 生成 Agent → `role_template` 自动沉淀**~~ | R2,A3a✅ | ✅ 机制已完成：`role_template` 表 + generated Agent 沉淀 + skill_refs/provenance/eval 元数据 + embedding backfill；复用率 gate 已补（Run Manifest node→Agent 名称 + `scripts/r4/role_template_reuse_gate.py`，默认排除 `meta.sample=true`、`M7验收门-*` 与 R4 smoke）；最小 live 样本脚本 `scripts/r4/seed_reuse_sample.py --write` + `--include-samples` 已打通并 PASS（1/1=100%）；自然复用集成测试已补（compose_agent→role_template→manifest→gate PASS）；dev/live 自然编配 smoke `scripts/r4/seed_natural_reuse_smoke.py --write` 可用 `--include-benchmark` 复现机制样本；当前默认真实业务口径为 `NO DATA`，剩真实用户业务运行样本验证 |
| P2(小补) | ~~R3 加固：即时 embedding 回写 + 回归测试~~ | R3✅ | ✅ 已完成；保存模板时 best-effort 同步写 embedding，补 `save-as-template` 幂等覆盖、可见性过滤、`catalog/templates?domain=` 筛选回归 |
| P3(小补) | ~~**S2b** local replan 控制面 + LLM 子图生成内核 + 在线 Activity 接线 + 自动策略~~ | S2a✅ | ✅ 已补失败节点+下游闭包替换拼接、`validate()` 与状态重置；`generate_subdag` 可生成 replacement nodes 并自修复；`generate_replan_subdag` Activity 已接入 workflow opt-in 路径；非 human/system、非 dangerous 且失败牵连下游闭包时自动启用；叶子失败/显式禁用/system/dangerous 均不触发在线 replan 的边界回归已补；新增 `scripts/s2_s3/stability_regression_gate.py` 本地稳定性 gate 并 PASS；剩更大业务样本 |
| P3(小补) | ~~**S3 公平队列 admission + 多槽优先级 dequeue**~~ | S3✅ | ✅ 并发满时 `task_run.pending` 入队；终态释放槽后 best-effort 按空槽数批量启动 pending；排序为显式 `task_run.priority DESC` → 短作业优先(`plan.estimated_cost_cents ASC`) → FIFO；迁移 `f1a2b3c4d5e6` 加 `task_run.priority` 默认 0，迁移 `0a1b2c3d4e5f` 加 `task.priority` 默认 0；任务级 priority 来源已接入（`task.priority` → run priority，创建/出图/运行/列表响应贯通）；SLA/任务类型自动映射已补（未显式 priority 时从 `inputs.sla` 与 `inputs.task_type`/`taskType`/`type` 派生，显式 priority 含 0 优先）；已纳入 `scripts/s2_s3/stability_regression_gate.py` 完整 gate（队列集成测试 6/6 PASS，含同 priority/同成本 FIFO + 120 条 pending 批量排序）；剩真实流量/更大业务样本复测 |
| P3(按需) | ~~**TD-032 剩余**：goal 端可达 + tool 草稿沙箱闸~~ | A2✅ | ✅ 已完成机制：A2 前能力提案 + `resolve_capability` 去重 + Skill 生成链；tool 草稿最小权限 + 本地 MCP 沙箱 + 人审墙 |
| P3(已补证据) | ~~**长任务恢复硬测**~~ | S4✅ | ✅ 已补 Temporal heartbeat timeout 恢复测试：activity 停心跳后触发 retry，workflow 恢复 done；human durable wait 已有 signal 测试 |
| P3(按需) | 记忆细化：B3 task→role 中间层 + 频次门；judge 双评/回归集再降噪 | B3✅ | 当前直接 task→org 蒸馏、rubric 已大降噪 |
| 延后 | 工程/上线：分支保护(TD-001) / 容器化(TD-009/029) / 生产安全(TD-013 邮件投递+分布式/边缘限流) / 成员管理(TD-016 邀请+移除) | 触发条件 | CI workflow 模板已落，远程启用待 workflow scope；登录失败进程内限流、找回密码前后端闭环、前端审批角色门控已落地；分支保护/容器化/生产安全/成员管理进 staging / 对外 / 团队>1 时继续 |

### 待清的门
- **阶段 C 门**：历史可追溯、附件、导出、看板、R3 场景模板沉淀、P5 场景树与 R4 自动沉淀机制已落；**R4 自动沉淀有效性≥60%** 的量化脚本、最小 live 样本闭环（`--include-samples` PASS 1/1=100%）、自然复用集成测试与 dev/live 自然编配 smoke 已补。2026-07-07 已收紧 gate 口径：默认排除 M7/R4 smoke，真实业务口径当前 `NO DATA`；`--include-benchmark` 诊断口径为 4/14=29%，说明 M7 验收门一次性模板不能当真实业务门，剩真实用户业务运行样本持续观察。
- B 稳态门**更大样本复测** ✅（当前 6 目标 full 100%；S2b/S3 本地稳定性 gate 已补，完整 PASS；M7 harness 已补 `needs_review` 终态识别、任务完成率输出、`--goals-file` 大样本入口；新增 `scripts/m7/goals_b_stability.json`。2026-07-06 dry：24 目标 DAG 95.8% / 路由 97.8%，1 条生成路径 120s 超时按单项失败记录；full 24 初跑达门但有残余：B 任务完成率 95.8%(23/24 done)、DAG 100%、路由 95.9%、人审通过率 95.7%、needs_review=0/failed=0/non_terminal=1、均成本 ¥0.0087、均时延 126.3s。第 19 条 non_terminal 已确认为 benchmark 未自动 signal human gate，harness 已补默认自动识别 `waiting_human`/`needs_human` 并 signal，live 复测 plan `33872b7e-9686-4826-a5f9-997ad64fec40` → `done`、`human_signals=1`。第 23 条外部 eval 低分已确认为 harness 优先取 281 字 `summary` 导致信息损失，已改为优先全文 `content`；live 复测 plan `76e3d601-c3aa-4a36-9b5d-ab9316bdefe1` → `out_len=7213`、`judge=0.95`、`pass=true`。2026-07-07 已重跑 full 24 刷新报告：**24/24 done、B 任务完成率 100%、DAG 100%、路由 92.9%、人审通过率 100%、needs_review=0/failed=0/non_terminal=0、human_signals=1、均成本 ¥0.0078、均时延 117.0s、预算内 100%**）。

> **建议路径**：下一步持续用真实业务 live 数据观察 **R4 角色模板自动沉淀自然复用率 ≥60%**，或继续工程化/上线类后置项（分支保护、容器化、生产安全等；CI workflow 模板已落，远程启用待 workflow scope）。

### 本批次 DoD / 门禁记录（按 docs/constraints）
- **DoR/DoD**：R3/P5/R4 均对应 V2 WBS 与设计章节；任务清单、README、续接指南已同步。
- **迁移与隔离**：R3/R4 迁移均走 Alembic；R3 私有模板可见性有跨 org 回归，R4 role_template 为 owner_org_id + visibility 资产层，后续复用查询需继续走 `visible_clause`。
- **S2b/S3 本地 gate**：`uv run python scripts/s2_s3/stability_regression_gate.py --keep-going` 完整 PASS（S2b workflow 11/11、子图生成 10/10、S3 队列 6/6）；无 Docker 可用 `--skip-docker` 先跑 S2b 子集。
- **已跑门禁**：后端 `ruff check .`、`mypy src`、全量 `pytest -q -p no:warnings`、`bandit -r src`、`gitleaks protect --redact`、`pip-audit`；前端 `pnpm exec tsc --noEmit`、`pnpm build`。
- **验证结果**：dev DB `alembic upgrade head` 通过；Docker testcontainers 集成测试 `tests/test_integration_composer.py tests/test_integration_templates.py` 通过；`pip-audit` 无已知漏洞（本地包 `polis` 正常跳过）。
- **已知偏差**：前端约束期望 React Query + OpenAPI 生成类型；当前项目基线仍是 `frontend/lib/api.ts` 手写 client，本批次按既有模式扩展，统一数据层需另立任务。

---

## C0 · 交互重构（IA 重排）— 档期已定（2026-06-30，对照原型）

> 目标稿 = `~/Downloads/Polis 可点击原型 (可下载).html`（Claude design 导出，已渲染确认）。
> 设计方向见 [产品交互重构（IA与主线）](../产品交互重构（待梳理·IA与主线）.md)。
> **关键事实**：后端能力（出图/任务/运行/观测/审批/花名册/用量/记忆）几乎全有，C0 ≈ **纯前端按新 IA 重排**，
> 仅需 1~2 个小 BE（「存为任务」、工作列表合并临时运行）。全部对照现有 API，不动数据模型。
> 原型 4 屏：① 工作台(home) ② 工作列表(list) ③ 工作详情+运行观测(workDetail，含内联审批) ④ 花名册(roster)。

| ID | 任务 | 对照原型/接现有 API | 依赖 | 验收标准 | 估时 | 轨 |
|---|---|---|---|---|---|---|
| C0-1 | **应用壳 + 工作台首屏**：左导航(工作台/工作/花名册/设置) + 公司切换器 + 「新目标」；工作台问候 + **目标输入框「出图」** + 快捷 chips | 工作台屏；`/api/me`、`POST /api/plans` | — | 登录即见工作台；输入目标→出图→进运行详情 | 3 | FE |
| C0-2 | **工作列表（任务+临时统一）**：tabs 全部/进行中/待处理/已完成 + 状态/最近运行/节点/成本/操作(去审批·查看进度·重试·再次运行·存为任务) | 工作屏；`/api/tasks`、`/tasks/{id}/runs`（+ 合并临时运行） | C0-1, BE-W1 | 一处看全部工作 + 历次记录；操作按状态正确 | 3 | FE |
| C0-3 | **工作详情 + 运行观测（核心）**：DAG↔观测并排 + tabs(计划与运行/完整日志/用量与成本) + **节点内联审批(通过/退回)** + 再次运行 | workDetail 屏；`/observability`、`/signal`、approval decide | C0-1 | 一屏看 DAG+产出+用量；关键节点直接审批；少 2~3 级点击 | 3 | FE |
| C0-4 | **工作台「需要你处理」+ 进行中 + 最近产出** 卡 | 工作台屏；`/api/approvals`、runs | C0-1 | 待审/在跑/近产出一屏可见可跳转 | 2 | FE |
| C0-5 | **花名册重排**：角色分组 + Agent 卡(能力 pill + 在岗/待上线) + 成员(所有者/审批人/成员) | 花名册屏；`/agents`、`/roles`、`/members` | C0-1 | 花名册清晰；能力/状态/成员角色可读 | 2 | FE |
| BE-W1 | **「存为任务」+ 工作列表合并临时运行**（小 BE）：临时出图运行可一键存为可复用任务；列表 API 合并 task ∪ 近期临时 run | 存为任务按钮 / 工作列表 | P1a✅ | 临时运行可存为任务；列表统一呈现 | 2 | BE |
| 门 | C0 验收：登录→工作台输入目标→出图→运行→一屏观测→（关键节点）内联审批→看产出，**全程 ≤ 3 层**；旧 dashboard/orgs 层级链下线 | — | 上列 | 用户「知道如何下手」、主路径不再深下钻 | — | — |

- **⚠ 布局硬约束（用户反馈 2026-06-30，实现时必须遵守）**：**右侧主内容区全宽自适应铺满，禁止出现大量右侧留白**（之前那版的问题）。以用户上传的三张正比例截图为准：
  - 壳：左导航固定宽(~240px) + 右侧主区**占满剩余宽度**，内边距 ~24–32px；容器**不设过窄 max-width**（或设很大，如 ≥1400px / 直接全宽留边距）。
  - 工作台：hero 卡全宽；「进行中 / 最近产出」**两栏等宽铺满**；「需要你处理」审批卡两栏（或全宽行）。
  - 工作详情：「执行计划(DAG)」与「运行观察」**两栏（约 1:1）等高铺满**，不留右白。
  - 工作列表：表格**全宽**，列宽合理分布（工作名占主，状态/最近运行/节点/成本/操作）。
  - 栅格随视口伸缩（响应式），窄屏可降级单栏，但宽屏务必铺满。
- **顺序**：C0-1（壳，先行）→ C0-3（详情，最高价值）∥ C0-2（列表，需 BE-W1）→ C0-4 / C0-5。BE-W1 与 C0-2 并行起。
- **估时**：约 15 人天（FE 13 + BE 2）；单人 ~3 周，可与后端 P2(附件/MinIO，纯后端) 并行。
- **完成后**：P2/P3/P4 直接在新工作台壳里挂（P3 执行记录≈C0-3 复用、P4 看板=工作台新卡 + 用量页）。

---

## 阶段 C · 就绪任务卡（进入产品化飞轮，2026-07-01 建卡）

> 阶段 C 起步。对应 [V2 进度快照](#v2-进度快照--后续优先级截至-2026-06-30) 的 **P2a**（WBS）。关键路径入口：纯后端、无前端依赖、可立即开工，解锁 P2b 附件消费与 P3b 结果导出。

| 字段 | 内容 |
|---|---|
| **任务 ID** | **T7.6**（INF 基础设施线，接 T7.1–T7.5） |
| **标题** | MinIO 对象存储接入 + 客户端封装 + `{org_id}/{task_id}/` 前缀多租户隔离 |
| **关联设计** | `docs/design/v2/05-产品形态` §9（对象存储引入·fork2）、§17（里程碑 P2）、§16.2（附件注入时序）；数据模型 `artifact_descriptor`（05§2 / T5.1 已建，`uri`→MinIO） |
| **依赖** | T0.3 docker-compose 起栈 ✅ · T5.1 `artifact_descriptor` DDL ✅ · B1 黑板 ✅（下游 P2b 用，本卡不阻塞） |
| **交付物** | ① `infra/docker-compose` 增 MinIO 服务（含 healthcheck）+ `.env.example` 补 `MINIO_*`（endpoint/access/secret/bucket，凭证走 env 不入库不入日志）；② `backend/src/polis/modules/storage/`（`client.py` 封装 put/get/presigned-url/delete + `key(org_id, task_id, name)` 前缀构造器，异常→领域异常）；③ 启动幂等建桶 + 前缀隔离校验；④ 单测（前缀隔离：org A 无法读 org B 前缀对象）+ `infra` 起停文档补 `docs/续接指南.md` |
| **验收标准** | 1) `docker compose up -d minio` 健康、控制台可达；2) 客户端可 put/get/删/签发临时下载 URL；3) key 恒为 `{org_id}/{task_id}/{name}`，**跨 org 前缀零可见**（单测断言 + 接 T8.3 隔离回归）；4) 凭证不出现在代码/日志/上下文；5) 门禁三件套（ruff/mypy/pytest）全绿 |
| **估时** | 2 人天（INF/BE） |

**DoR 自检**
```
☑ 目标/验收标准明确   —— 交付物 + 5 条验收量化清晰
☑ 关联设计章节       —— v2/05 §9/§17/§16.2 + artifact_descriptor(05§2)
☑ 依赖已就绪         —— docker-compose(T0.3)/artifact 表(T5.1) 均已合并 main
☑ 无重大未决问题     —— 见下「已决口径」
☑ 1~3 天内可完成     —— 2 人天，无需拆分
```
→ **状态：✅ 已完成（2026-07-01）**。交付：`modules/storage`（ObjectStore：put/get/presigned/delete + `object_key` 前缀校验）、compose minio 服务(9000/9001)、config `POLIS_MINIO_*` + env 模板、`test_storage_prefix`（单测穿越校验 + testcontainers MinIO 隔离集成测）、ADR-0014 置 accepted。门禁三件套全绿（ruff/mypy 74/pytest 149）；实测 compose healthy + 9000/9001 可达 + 客户端回环通 + 前缀隔离成立。**P2b 起可用**（接 input_schema 上传 → artifact → 黑板注入）。

### 任务卡 · P2b 附件消费（WBS P2b，拆两刀）

| 字段 | 内容 |
|---|---|
| **任务 ID** | **P2b**（planner/runtime 应用层，接 T7.6） |
| **关联设计** | v2/05 §5（输入/附件入口）、§16.2（附件注入时序）；`artifact_descriptor`(modality='file') |
| **依赖** | T7.6 对象存储 ✅ · P1 task 表 ✅ · B1 黑板 ✅ |

- **P2b-1 附件上传入口** ✅（2026-07-01）：`POST/GET/DELETE /api/tasks/{id}/attachments` + `GET .../{name}/url`（预签名 15min）。
  上传落 MinIO `{org}/{task}/{文件名}`（basename 消毒防穿越）+ 登记 `artifact_descriptor`（modality='file'、归属记 `meta.owner_task_id`，同名覆盖）。
  ObjectStore 做成可 override 的 FastAPI 依赖（端点脱离 live MinIO 可测）。交付：`storage/deps.py`、planner `repository` 附件 CRUD、
  `AttachmentOut/AttachmentUrlOut` schema、`test_integration_attachments`（生命周期 + 任务前缀隔离 + 上传守卫，假 store）。
  25MB 上限；依赖补 `python-multipart`。门禁全绿；**实测 live compose MinIO 端到端**：上传 201→预签名下载内容正确→删除 204。
- **P2b-2 运行时注入** ✅（2026-07-01）：黑板扩展——`resolve_owner_task_id`（task_run.id 反查所属可复用 task.id，
  附件挂在任务而非某次运行上）+ `attachments_brief`（清单默认注入：文件名/字段/类型 + 懒加载提示，org 隔离）+
  `read_attachment(filename)` 懒加载正文（镜像 `read_node_output`；文本类解码返回，二进制类仅元信息提示，不做二进制解析 MVP）。
  `ExecCtx.attachments_brief` 新字段，`run_loop` 注入 user 段（`input_hint`→`deps_brief`→`attachments_brief`→`memory_slice` 优先级，
  统一受 B4 ctx_budget 截断）。`agent_runtime.execute` 接线：解析 owner_task_id → 注入 brief → `ToolCtx` 带 `attachment_task_id`。
  `run_task` API 加 `_require_attachments`：`input_schema.attachments[*].required` 声明的必填附件未传齐 → 422 报缺失 label（前置在
  service.plan/Temporal 之前，不依赖外部服务即可测）。
  测试：`test_integration_attachment_runtime`（清单注入+隔离、read_attachment 文本/二进制/缺失、owner 反查、
  **端到端**：`agent_runtime.execute` 真跑 + `StubModelGateway` 确定性回显验证附件清单进了 prompt）+
  `test_integration_attachments::test_run_task_requires_declared_attachments`（422 门 + 补传后放行）。
  门禁全绿（ruff/mypy 75/pytest）。**验收达成**：DoR「上传附件被任务正确读取并影响产出」✅。**P2b 全部完成**。

**已决口径（消除未决）**：桶名固定单桶 `polis`（多租户靠 key 前缀，不按 org 建桶）；`artifact_descriptor.uri` 存 `s3://polis/{org_id}/{task_id}/{name}`；下载用 presigned URL（短时效），不做公开读；MVP 不引 S3 生命周期/版本化（后置）。
**待落 ADR**：ADR-0014（任务为独立可复用实体 + 引入对象存储 MinIO）——本卡落地时同 PR 补写（设计已提「待落」）。

---

### 任务卡 · P3 执行记录 + 结果导出（WBS P3a/P3b）✅（2026-07-01）

| 字段 | 内容 |
|---|---|
| **任务 ID** | **P3a**（FE）+ **P3b**（BE，接 T7.6 对象存储） |
| **关联设计** | v2/05 §6（执行记录）、§7（结果存储与展示）、§17.4 |
| **依赖** | P1a task 表 ✅ · T7.6 对象存储 ✅ |

- **P3b 结果导出** ✅：`planner/export.py`——`build_markdown` 拼装标题/状态/各节点产出/用量成本；
  `render_pdf` 用 reportlab **内嵌真实 TrueType 中文字体轮廓**渲染（`_ensure_cjk_font` 按 `POLIS_PDF_CJK_FONT_PATH` 或常见路径探测，
  找不到报 `ExportError`→503，绝不静默产出空白 PDF）。
  ⚠ **实测踩坑**：reportlab 内置 CID 引用式字体（如 `STSong-Light`）**不内嵌字形**，依赖阅读器本地装有对应中文字体——很多环境
  （无中文字体的 PDF 渲染器）会把中文渲染成空白；改用系统/装机 TTF（如 `Arial Unicode.ttf` / 装 `fonts-noto-cjk` 后的 `.ttc`）
  内嵌真实轮廓后，任何阅读器都能正确显示（已用 Read 工具截图逐字核验）。
  API：`POST /api/plans/{plan_id}/export?fmt=md|pdf`——`_gather_run_data` 复用/重构自 `get_plan_observability`（消重复查询）
  → 渲染 → 落 MinIO(`repo.create_export_artifact`，task_id 用 **run.id** 而非附件的 task.id 语义) → 直接 `Response` 返回文件下载
  （`Content-Disposition: attachment`）。`storage/deps.get_object_store` 补 `StorageError→HTTPException(503)`（原缺陷：依赖注入阶段抛的
  异常不会被路由体内的 try/except 捕获，会裸 500——已修，惠及附件+导出两条线）。
  测试：`test_integration_export`（md 内容正确性/400 非法 fmt/404 未启动/pdf 200 或环境无字体优雅 skip + `build_markdown` 单测）。
  **实测 live**（真实 MinIO + 真实浏览器点击）：md/pdf 导出 200，PDF 中文正确渲染，Markdown 内容含节点产出。
- **P3a 执行记录列表** ✅：工作列表「节点」列（运行次数）改可点击（`.linklike`），弹 `Modal` 列出该任务**全部历次运行**
  （之前 C0-2 重排后只能看最新一次，此为补回的缺口）——每条运行显示状态/时间/成本 + 「查看观测」（复用 `plans?plan=` 详情页，
  M6-H-4 观测页原样复用，不新建详情页）+「导出md/pdf」（直接调 `api.exportPlan`）。
  `api.ts` 新增 `exportPlan`（blob fetch，非 JSON）+ `downloadBlob` 辅助；`plans/page.tsx` 头部加导出按钮
  （门控条件 `obs && obs.nodes.length>0`——语义是"有产出可导出"，不依赖 Temporal 的 `run` 轮询状态，因为后者在 Temporal
  未连通时会 503 但观测数据本身可能已存在）。
  **实测 live**（真实浏览器点击，非仅 API 调用）：详情页头部导出 + 工作列表执行记录弹窗导出，两处 md/pdf 均 200，
  浏览器触发下载。
- **踩坑记录（环境）**：调试中发现 `frontend/.env.local` 的 `NEXT_PUBLIC_API_BASE` 指向局域网 IP（会变、不稳定）导致间歇性
  连接超时——按用户要求改回 `http://localhost:8000`（`NEXT_PUBLIC_*` 改动需重启 dev server 生效，非热更新）。

**验收达成**：DoR「结果一键导出 md/pdf 并下载」✅、「任意任务可查全部历次执行记录」✅。门禁全绿（ruff/mypy 76/pytest + 前端 tsc 0 错）。**P3 全部完成**。

---

### 任务卡 · P4 看板（WBS P4）✅（2026-07-02）

| 字段 | 内容 |
|---|---|
| **任务 ID** | **P4**（planner/observability 应用层） |
| **关联设计** | v2/05 §8（Dashboard/运营统计） |
| **依赖** | P3 ✅ · S3 ✅（并发闸/预算提示） |

- **后端** ✅：`GET /api/dashboard`（org 级聚合，`DashboardStats` schema）——
  - 状态分布 + 成功率（done / 全部终态[done+failed+needs_review]，`dashboard_status_counts`，DB `GROUP BY`）；
  - 平均耗时（`dashboard_avg_duration_seconds`，`AVG(extract(epoch from finished_at-started_at))`）；
  - **复用命中率**（飞轮指标）+ **按场景分布**（`dashboard_template_distribution`，按 `Plan.version`（可空）原始列分组——
    NULL 天然聚成"生成"一组，避免多表达式 `GROUP BY` 在 Postgres 下的严格匹配报错，命中/名称在 Python 侧派生）；
  - **人审通过率**（`obs_repo.approval_decision_counts`：`approved/(approved+rejected)`）；
  - 进行中运行数 + 并发上限（复用 S3 `count_active_runs`）、预算使用提示（复用 S3 `org_estimated_cost_cents`）；
  - 近期窗口（默认 50 条）实测成本/token（逐条拉 Langfuse，限量避免全量聚合过慢，复用既有 `_fill_actual_cost` 模式）。
  - 重构：`_gather_run_data` 单次运行取数与本卡的 org 级聚合分离，无重复代码。
- **前端** ✅：`/orgs/[id]/dashboard` 新页（AppShell 加「看板」导航项 + 图标）——统计卡（`dash-stat-row`，
  `auto-fit/minmax` 自适应列数，避免中文长标签在窄容器下逐字换行）+ 状态分布/场景分布横条图（`dash-bar-*`）+
  空态引导（零运行时提示去「工作」出图）。
- 测试：`test_integration_dashboard`（4 条运行+2 条已决 approval 断言各聚合数值精确正确；空 org 优雅返回 null/0，不报错）。
- 门禁全绿（ruff/mypy 76/pytest + 前端 tsc 0 错）。**实测 live**：真实浏览器渲染，5 条运行(3done/1failed/1needs_review)+
  3 条 approval(2approved/1rejected) 场景下，成功率60%/复用命中率80%/人审通过率67%/状态·场景分布条形图均与预期精确一致；
  空 org 空态文案正确展示。

**已决口径**：成功率/复用命中率/人审通过率的分母仅取"终态"或"已决"数据（不含 pending/running 等未决状态），
避免进行中任务稀释统计意义；近期窗口成本/token 聚合暂不做全量历史（性能考量，MVP 合理简化，后续如需可加缓存/后台预聚合）。

**验收达成**：DoR「成本/成功率统计与明细一致」✅（数值经测试精确断言）。**P4 全部完成**。
