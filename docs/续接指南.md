# Polis 续接指南（新 session 快速接手）

> 新开 Claude Code 会话时：**把工作目录开在 `/Users/lining/workspace/polis`**（不要在别的项目根里隔空操作 Polis）。
> 这样会自动加载本仓库 `CLAUDE.md`，且 cwd/git/子代理都对得上。先读本篇 + `docs/README.md`。
> 维护：每个里程碑/批次结束时更新本篇的"进度"与"下一步"。最近更新：2026-07-04（S2b 自动 replan 边界回归）。

---

## 1. 这是什么
Polis · **虚拟智能企业平台**：给定目标，开出一家 AI 虚拟公司（Org）——角色化 Agent 由 Planner 规划、按能力路由、在受治理（审批/护栏/凭证隔离）的运行时协同完成。复用优先（Temporal/LiteLLM/MCP/pgvector/Langfuse），自研差异化粘合。
设计文档全集在 `docs/design/`（00–09 + 0a 理念 + 0b 数据模型），约束在 `docs/constraints/`，决策在 `docs/decisions/`，计划在 `docs/plan/`，技术债在 `docs/tech-debt.md`。

## 2. 里程碑进度
- **M0 脚手架** ✅ 合并 main（FastAPI 骨架 + uv + pre-commit 门禁 + docker-compose 基础设施 + Alembic 基线）
- **M1 数据底座/身份/多租户** ✅ 合并 main（27 表全量 DDL + RLS + 身份认证 + 词表/目录/预设 seed + 登录页/看板）
- **M2 立邦闭环** ✅ 合并 main（当前公司中间件+运行时 RLS、预设→实例化花名册、公司编辑/删除/成员/切换器、前端）
- **M3 大脑** ✅ 合并 main（确定性「大脑」端到端跑通：意图→DAG→编排→运行）：
  - **M3-A** ✅ Plan(DAG) schema + 校验器 `validate()` + 计划模板种子（`3876b2d`）
  - **M3-B** ✅ 模板优先 Planner + 确定性 Router + `POST /api/plans`（`58a5880`）
  - **M3-C** ✅ Temporal TaskWorkflow（串/并/human-gate/有界重规划）+ 运行/审批 API（`9048e82`）
  - **M3-D** ✅ 前端任务/计划页（DAG 分层预览 + routing/成本/校验状态 + 批准运行 + 轮询徽章 + human-gate「通过」+ 503 降级）（`0317a7f`）
  - **M3 收尾** ✅ 真实 Temporal 端到端联调（workflow Completed、串/并/汇聚徽章正确）；
    修 `GET /run` 顶层状态派生+终态回写（`3f524b1`）；补 approve/signal 的 owner/approver 角色守卫（`80ac672`）；
    登记 TD-018/019。
- **M4 执行运行时** ✅ 桩驱动执行内核（ADR-0007：真实 LLM/记忆/凭证延后 M5/M6）：
  - **M4-A** ✅ ModelGateway 接口 + StubModelGateway 确定性桩（`bacb346`）
  - **M4-B** ✅ SkillLoader 双形态 + 最小权限过滤；AgentConfig 扩 model/authority（`4f30026`）
  - **M4-C** ✅ McpRegistry/McpRuntime + 内置本地工具 echo/calc（`afb3f99`）
  - **M4-D** ✅ ContextAssembler 三样注入 + lite-agent _loop 多轮+超步；MemoryCenter/Credential 桩（`bc73274`）
  - **M4-E** ✅ Guardrails 三道防线（注入检测+回流过滤，接入 _loop）（`94036a4`）
  - **M4-F** ✅ AgentRuntime.execute + run_node 接真实执行 + ResultEnvelope 出处入库（端到端测试通过）
- **M5 记忆** ✅ 确定性记忆系统（embedding/向量检索延后 M6，复用 ADR-0006/0007）：
  - **M5-A** ✅ ModelGateway.embed 接口+桩 + memory/repository.py
  - **M5-B** ✅ 写入管线 write_facts（抽取/评分/去噪去重/出处入库）；AgentRuntime 接入
  - **M5-C** ✅ 检索管线 retrieve（作用域过滤+关键词相关性+importance/recency 排序+摘要）；ContextAssembler 接入
  - **M5-D** ✅ decay_job 衰减遗忘 + CLI + upsert_shared_fact 并发裁决（覆盖/冲突标记）
  - **M5-E** ✅ memory 治理 API（GET 浏览/DELETE）+ 读写审计 + org 行级隔离回归
  - **M5-F** ✅ 与 04 联调：执行→写回→再检索闭环（第二 Agent 读到第一 Agent 带出处事实）
- **M6 模型接入·凭证·观测·评估** ✅ 真实化收口（DeepSeek chat + 本地 bge embed 真调）：
  - **M6-A** ✅ embedding 本地 TEI(arm64 华为云镜像, bge-large-zh-v1.5, 1024) + model_catalog(deepseek-v4-pro/flash) + config/KMS
  - **M6-B** ✅ Credential Broker 信封加密(Fernet DEK+KMS) + scoped 短时句柄 + POST /api/credentials
  - **M6-C** ✅ LiteLLMGateway chat/embed 真调 + cost_aware_pick 成本路由
  - **M6-D** ✅ 桩切真：execute_node 真实 gateway + 向量 RAG；🎉 实测 deepseek chat + bge embed 1024 + 单节点 execute 真实输出
  - **M6-E** ✅ 模型配置前端（owner 选模型+填Key→信封加密；浏览器+DB 验证密文入库）
  - **M6-F** ✅ Run Manifest 落库复现（approve 时落 plan 快照/模型/agent）
  - **M6-G** ✅ 审批收件箱 approval（五类 kind 统一队列 + decide→Temporal signal）
  - **M6-H** ✅ Evaluator（断言+LLM-judge+回归集 + POST /api/eval/run）
  - **M6-H-1** ✅ Langfuse 容器**开箱即用**（headless 预置 keys，端口 3001，零手动配置）+ LiteLLMGateway 自动上报 trace
  - **M6-H-2** ✅ 运行观测聚合 API（GET /api/plans/{id}/observability：manifest+节点产出+Langfuse LLM 明细）
  - **M6-H-3** ✅ 前端「运行观测」页（Polis 风格，自建非 Langfuse UI）；TD-028 task_run 接线已补
    （🎉 实测：采购 4 节点真实 DeepSeek 执行→观测页展示真实产出 + 4 次 LLM 调用 token 数）
  - **M6-H-4** ✅ **观测页产品化 UX 重构**（实测：TD015 org 真实采购 4 节点 DeepSeek 跑通，观测页全绿）：
    1. ✅ **DAG 流程动态图示**：`plans/page.tsx` 改 **SVG 有向流程图**（foreignObject 节点框 + 三次贝塞尔依赖箭头连线，
       状态色：待执行灰/执行中蓝(发光)/完成绿/失败红/待人审橙），轮询(1.5s)**实时变色**；保留 图/表 切换（表=旧分层卡片回退）。
    2. ✅ **节点产出可读性**：`react-markdown` + `remark-gfm`（GFM 表格必须 remark-gfm，否则管道符原样显示）+ `<details>` 折叠卡。
    3. ✅ **token/成本统计**：后端 `langfuse_client.fetch_generations` 补 in/out/total tokens + cost；
       `planner/api.py:_aggregate_usage` 出 `totals`(总 token/成本/调用次数) + `by_model`；前端统计数字卡 + 按模型表，去掉原始返回堆叠。
    - 注意：langfuse 对自定义模型 `deepseek-v4-pro` 无定价表 → `cost` 返回 null，前端优雅显示 `$0.00000`/`—`（token 正常）。
    - 进阶（仍可选，未做）：节点级 token 归因——execute 传 node_id → langfuse generation name=node_id。
  - **M6-H-4 二轮（按用户新设计稿对齐）** ✅：节点卡加**图标**（按能力归类 rfq/supplier/spend/report）+ **能力 pill**（procurement.rfq 等，替代 lite-agent）
    + **节点时间戳** + **依赖**；列表视图改 **第N层时间线**（check/dot 标记 + 竖虚线）；观测卡加图标/时间/折叠箭头 + **查看完整日志**；
    底部统计 **总耗时**（用户选「保留但收纳折叠」→ token/成本统计移入可折叠「用量统计」区，默认收起）。
    - ⚠ TD-019 未还：`task_run.started_at/finished_at` 仍未回写 → `/observability` 的 `duration_seconds` 用**节点产出时间兜底**（min/max envelope.created_at）；TD-019 还清后可直接用 started/finished。
  - **M6-H-4 三轮（用户反馈四点）** ✅：
    1. **DAG↔观测联动**：点左侧节点 → 右侧对应观测卡展开+滚动到视野+高亮（`selectedNode` 受控 `<details>`，`onSelect` 贯通图/表两视图）。
    2. **查看完整日志**：弹模态 `LogModal`（节点产出 markdown + LLM 调用明细 in/out 全文 + 每次调用 token/成本）。
    3. **DAG 构建方式**（答疑，非改动）：当前 **模板优先确定性**（`planner/service.py` 匹配 plan_template，固定图）；**LLM 全自动拆解兜底是 TD-020，仍 open 未做**。
    4. **实际费用**：执行完成后底部卡 预估费用→**实际费用**（`planner/api.py:_fill_actual_cost` 按 `model_catalog.price_in/out`(元/1K token)×真实 token 实算，单位**元¥**，覆盖 langfuse 的 null cost）；用量统计/按模型表/每次调用成本同步显示 ¥。
  - **M6-H-4 四轮（用户标注图四点）** ✅：
    1. **批准并运行**按钮从顶部 plan-meta 移到 **DAG 面板头右侧**（图/表旁），做成醒目主按钮 `.btn-run`。
    2. **运行前状态文案** `idle`「待出图」→**「待确认执行」**（`STATUS_LABEL.idle`）；其余态不变。
    3. **节点卡补 Agent 说明 + 点名称弹「Agent 详情」模态**：卡片加 agent 职责描述；点 agent 名（`.agent-link`）弹 `AgentModal`（名称+角色+在岗/来源/版本+职责说明+能力清单）。
       数据：扩 `GET /orgs/current/agents`（`repo.list_agents_detailed`）—— 描述取 `agent_version.config.prompt`(promptSkeleton)，能力/模型取 config，角色 join role；前端 onMount 拉花名册建 name→agent 映射（routing 存 agent 名）。
    4. （答疑）DAG 仍**模板优先固定图**，LLM 自动拆解=TD-020 未做。
    - graph 节点高 NODE_H 提到 142 容纳描述行；两视图均生效。
- **M7 端到端验收门**（V1 验证门 F3）🚧 进行中：
  - **T8.2 harness + 目标集** ✅：`backend/scripts/m7/acceptance_gate.py` + `goals.json`（12 个采购目标）。
    驱动 plan→approve→run→observe→eval，量化四指标。两模式：`--dry`（仅出图，零 LLM，秒级）/`--full`（真实 LLM 跑+评测）。已兼容 V2 终态：`needs_review` 不再轮询到超时，报告会额外输出 B 稳态任务完成率（done/approved）与 needs_review/failed 计数。
    用法：`uv run python scripts/m7/acceptance_gate.py --dry`（或 `--full [--limit N] [--org-id <已有花名册公司>]`）。
    B 稳态更大业务样本：`uv run python scripts/m7/acceptance_gate.py --dry --goals-file scripts/m7/goals_b_stability.json`；
    生成路径较慢时可调 `--request-timeout 180`，单目标超时会记失败并继续后续目标。
    2026-07-06 已跑 dry：24 目标，DAG 可用率 95.8%，路由命中率 97.8%；1 条生成路径 120s 超时，已记录为单项失败且整批继续。
    已跑 full 小样本：`--full --limit 2 --goals-file scripts/m7/goals_b_stability.json --request-timeout 180 --timeout 420`
    → 2/2 done，B 任务完成率 100%，judge 0.95/0.95，均成本 ¥0.0054，均时延 117.4s，预算内 100%。
    缺 `--org-id` 时用预设新建一家「M7验收门-*」公司。需后端 + worker + temporal + DeepSeek Key 在跑。
  - **T8.3 多租户隔离回归** ✅：`tests/test_integration_tenant_isolation.py`（API 层 A/B 互不可见，进门禁）+ 既有 `test_integration_rls.py`（DB/RLS 层）。
  - **T3.7 验收门量化** ✅ **🎉 V1 验收门通过**（12 目标 × 真实 DeepSeek，org `c5b59c0d`）：
    | 指标 | 实测 | 门槛 | |
    |---|---|---|---|
    | DAG 可用率 | 100% | ≥70% | ✅ |
    | 路由命中率 | 100% | ≥75% | ✅ |
    | 人审通过率 | 66.7%（8/12） | ≥50% | ✅ |
    | 成本/时延 | 均 ¥0.0154 · 37.8s · 预算内 100% | 预算内 | ✅ |
  - 注意：DAG可用率/路由命中是确定性的（模板优先），采购单线下恒 100%——真正考验在**人审通过率**（LLM 产出质量）。
  - **F3 回炉（不加模块，已落地）** ✅ 人审通过率 **66.7%→91.7%（11/12）**：
    1. **根因**：plan goal 从未传到 Agent——`context.build` 的目标只取节点静态 `expected_output`，所以不管问什么都套同一套模板叙事（支出/质量类目标答非所问→judge 低）。
    2. **修复**：把用户 goal 贯通 `workflow.run_node→execute_node→execute→context.build`（agent 系统提示「目标：{goal}」锚定用户意图，记忆检索也并入 goal）；
       强化 seed 预设的 询价/分析/报告 promptSkeleton（紧扣用户目标、按维度取舍、给量化可执行结论）。**改了 worker 代码 → 已 `make seed` + 重启 `make worker`**。
    3. **代价**：报告更厚 → 均成本 ¥0.0154→¥0.023、均时延 37.8→104.6s（仍预算内）。
    4. harness 补 per-goal relogin（长跑 access JWT 15min 会过期）。

- **V2（能协同）🚧 阶段 0 进行中**（设计/计划见 `docs/design/v2/` + `docs/plan` V2 章节 + ADR-0009~0014）：
  - **B1 任务黑板** ✅：`result_envelope` 加 `content`(全文)/`tokens`；`runtime/blackboard.py`（依赖摘要默认注入 + 确定性技能 `read_node_output` 懒加载，修 F3 节点协同）；`McpRuntime` 支持 ctx-aware async 工具。
  - **R1 检索底座+可见性** ✅：`plan_template` 加 `visibility`/`owner_org_id`、`skill` 加 `visibility`/`embedding`+hnsw；`db/org_scoped.visible_clause/select_visible`（私有∪public 读过滤）；`make embed-backfill`（capability/plan_template/skill 空 embedding 回填，需 TEI）。
    - ⚠ **R1b（RLS 读放行 public + 隔离回归）随首个私有资产写入（R2）一起做**——当前目录全为 public，无泄露面；私有资产用 `select_visible` 在查询层过滤（这些是全局目录表，本就无 org RLS）。
  - **P1 任务实体** ✅（后端）：新建 `task` 表（org 级 + RLS）+ `task_run.task_id`（1 任务:N 运行）；
    API `POST/GET /api/tasks`、`POST /api/tasks/{id}/run`（出图→快照 plan→启动编排，记一条执行记录）、`GET /api/tasks/{id}/runs`；
    `approve_plan` 与 `run_task` 共用抽出的 `_start_plan` 启动逻辑。`plan` 语义退为"某次运行的 DAG 快照"。
  - **P1b 前端** ✅：`/orgs/[id]/tasks` 任务页（新建/列表/运行/执行记录 + 查看观测模态，复用 markdown 渲染）；org 页加「任务 →」入口。api.ts 加 Task/TaskRun 类型 + createTask/listTasks/runTask/taskRuns。
    - ⚠ 验证用临时 org 成员行已清理；**dev DB 已 `alembic upgrade head`**（B1/R1/P1 迁移已应用到 localhost polis 库）。
  - **S1 状态机 + 质量门** ✅：节点态加 `needs_rework`、顶层加 `needs_review`（derive_overall_status）；
    新增 `evaluate_node` Activity（Evaluator 评分，worker 注册）；`_exec_node` 对**关键节点**（能力含 report/generation 或 `evaluate=True`）质量门——judge 不达标→needs_rework（S1 不再调度，S2 接纠错）；
    迁移 `c4d5e6f7a8b9`（task_run/plan CHECK 加 needs_review）；test_workflow 加 `force_rework` 钩子 + 质量门用例。实测：真实采购 4 节点 done（好产出过门）；门禁全绿。
    - ⚠ **改了 worker 代码已重启**；前端 STATUS_LABEL 加 待复核/待返工。
  - **🎉 阶段 0（地基）完成**（R1/B1/P1/P1b/S1）。
  - **A1 检索升级（第一刀）** ✅：`service.plan` 选模板从「第一个能力可行」改为「能力可行候选中按 goal↔模板向量最相似择优」（`repo.rank_plan_templates_by_goal` 走 hnsw 余弦），TEI 不可达/未注入网关/未回填 embedding → 优雅回退原确定性逻辑。单测 `test_plan_semantic_select.py`（6 例：语义择优/跳过不可行/各兜底分支）；实测 dev 回填模板 embedding 后 live 出图走语义路径正常。**能力/技能/角色语义检索按用户决定切片后置 → TD-030**。
  - **状态回写修复（TD-019 偿还）** ✅：`TaskWorkflow.run()` 收尾跑 `finalize_run` Activity 主动回写 task_run/plan 终态（含 finished_at），不再依赖客户端轮询 `GET /run`（任务页只读 DB/产出，旧实现会让运行卡 running）。
  - **A2 规划生成（偿还 TD-020）** ✅：`planner/generator.generate_dag`——模板未命中（相似度 < `TAU_TPL=0.78` 或无可行模板）时，RAG 接地 LLM 生成 DAG（top-k 模板骨架作范例 + active 能力词表约束）+ 结构(Pydantic)/语义(`validate`)双校验 + 有界自修复(N=2)，仍不过→`PlanInvalid`(422)；无 active 能力→`NoTemplateMatch`(404)。`service.plan` 重构为 检索(τ判命中)→生成→兜底 退化链。单测 `test_plan_generator.py`(5) + `test_plan_semantic_select.py`(6，含 numpy embedding 回归)。实测 live：营销渠道目标→生成 4 节点合法 DAG（能力闭合、无环、全路由，~20s 真 DeepSeek）。
  - **A2 验证门（in-process）** ✅：`scripts/a2/generation_gate.py` + `goals_a2.json`（10 个采购域内、单模板未覆盖的目标）。直接驱动 `generate_dag`（真实 DeepSeek），精确计每目标尝试轮次。**实测（flash + DB 范例接地）：生成 DAG 可用率 10/10=100%（≥70%）、结构合法率一次过 10/10=100%（≥80%）、平均尝试 1.00、平均 2.7 节点 → ✅ PASS**。说明：目标集是「能力可做但无模板」的 in-scope 目标，100% 反映 happy-path 生成质量；越界目标应优雅 422（未纳入分母）。
    - ⚠ **模型坑（已修）**：`.env` 的 `POLIS_DEFAULT_CHAT_MODEL` **覆盖** `config.py` 默认值——之前 config 改成 flash 但 `.env` 仍是 `deepseek-v4-pro`，运行实际用 pro。已把 `.env` 改为 `deepseek-v4-flash`（`.env` 不入库，故只本地生效；改 `.env` 后须**重启后端**才生效）。
  - **A3 编配生成（拼装路径）** ✅：`planner/composer`——`route_or_compose(dag)` 逐 agent 节点先 `select_agent` 检索现成 Agent，无→`compose_agent` 拼已审 Skill 成 Agent（§5.2–5.3，ADR-0010 任务驱动弹性）。`compose_agent`：每能力取一个 published+可见 Skill（按 trust 优先），全有→建 `source='generated'`/`status='active'` 的 Agent（名 `弹性组队·{caps}` 确定性→幂等复用；config 记 provenance.composed_from；回填 agent_capability），缺任一 Skill→None（缺 Skill 生成草稿+人审延后＝**TD-032**）。`available_capabilities(session, org_id)` 扩为 **active Agent 能力 ∪ 可见 published Skill 能力**（ADR-0009，让生成可瞄准「有 Skill 无 Agent」的能力→拼装兜底）。`service.plan` ④ 路由改用 `route_or_compose`。`AgentConfig` 加 `provenance` 字段。
    - 测试：`test_integration_composer.py`（拼装/幂等/缺Skill→None/route_or_compose 映射）；全量回归绿（70 源文件 mypy 0）。**实测 live**：org 私有播一个 `market.sentiment` published Skill（无对应 Agent）→ goal「社交媒体舆情口碑报告」生成 DAG，n1=market.sentiment 被 `弹性组队·market.sentiment`（拼装）覆盖、n2=report.generation 走现成报告Agent；已清理实验数据。
  - **A4 自动背书（advisory）** ✅：`composer._endorse` 在拼装后用 `evaluator.score` 给一份「胜任度」judge 快照写进 `AgentConfig.eval`（judge/passed/at/kind=compose_fitness），`route_or_compose`/`service.plan` 透传 gateway。**关键判断**：该 judge 只见技能名+岗位说明（非「试产出」）是弱信号（实测占位 Skill judge≈0），故改为 **advisory 不硬门控**——拼装 Agent 仍直接 active，eval 快照仅供观测/「采纳率」基线；**权威背书是 S1 执行期质量门**（对真实产出 judge→needs_rework，已实现）。基于「试产出/执行 eval」的硬降级＝**TD-033**。`AgentConfig` 加 `eval` 字段。`热路径阈值闸`=S1（已实现）；`模板沉淀`=用户满意度 UI（设计 §13.3，归 #6/#7，本块不做）。
    - 测试：`test_integration_composer.py::test_compose_endorse_snapshot`（高/低分快照均 active + 记录正确）；全量回归绿。实测 live：market.sentiment 拼装 Agent active 且 `config.eval` 记录 judge（advisory）。
  - **整链路验证（A 阶段收口）** ✅：M7 验收门跑全栈 `检索→生成→校验→编配→背书→执行→质量门→终态回写→评测`。`--dry`(12目标) + `--full`(3目标) **4 指标全过**：DAG 可用率 100% / 路由命中 100% / 人审通过 100% / 成本¥0.0181·时延122s·预算内100%（`--full` 还确认 S1 质量门产出 needs_review、A4 eval、finalize 回写都在跑）。
  - **A1 检索校准（第二刀，验证驱动）** ✅：整链路验证暴露**检索命中率=0%**（12 目标全走生成、没命中模板）。根因用数据定位：① 模板 embedding 源是英文标识符为主（`name+workflow_name`），对 bge-**zh** 是噪声→同域目标仅 0.50–0.70；② `τ_tpl=0.78` 偏高。修复：`embed_backfill._tpl_text` 改聚合**中文**（acceptance_criteria + 各节点 input_hint/expected_output），`service.TAU_TPL` 0.78→**0.50**（实测同域 0.57–0.69 / 跨域 0.36–0.41，两侧 ≥0.07 余量）。**重跑 backfill + --dry → 检索命中率 0%→100%**（12 目标全命中 supplier_analysis_v1、route 4/4，且免生成更快更省）。⚠ 改了模板 embedding 源后，dev 库已 null+重回填；**新库 seed 后需 `make embed-backfill`**。
  - **TD-032 Skill 生成链** ✅(机制)：`planner/skillgen.py`——`generate_skill_draft(cap)` LLM 写 manual playbook → `Skill(draft/private/org)` + `SkillVersion` + `skill_review` 审批（**安全红线：tool 不自动发布**）；`publish_skill` 仅人审通过后置 published/verified（能力进 available）。`compose_agent` 缺 Skill→生成草稿撞墙（None）；approval decide approve+skill_review→自动发布。测试 `test_integration_skillgen.py`。**tool/MCP 草稿沙箱闸已补**：`create_tool_skill_draft` 强制最小权限 + 本地 MCP 沙箱试跑，过闸后仍 pending 人审，`publish_skill` 拒绝未过 sandbox 的 tool；`test_skillgen_tool_sandbox.py` 覆盖无 DB 核心安全逻辑。**goal 端可达已补**：模板未命中进入 A2 前，最多 2 个缺失能力提案经 `resolve_capability` 去重后接 Skill 生成链，自动发布的 manual 能力同轮进入规划；集成测试覆盖空能力 org 的 goal→新能力→Skill→DAG→Agent 路径。
  - **TD-033 试产出 eval 硬降级** ✅：`composer._trial_endorse` 把 A4 的 advisory(只见技能名→judge≈0 会误杀) 换成「**试产出**」——拼装 Agent 带技能 playbook 正文产一份示例结果再 judge，judge≥0.6→active / <0.6→draft+None(硬降级)。实测：带 playbook 的 market.sentiment 拼装 judge=1.0(active)。无副作用(不落 envelope)；运行期仍有 S1 兜底。`compose_agent` 恢复硬门控 + draft 幂等。
  - **风险分级放行（ADR-0009 修订）** ✅：回应「每个生成 skill 都人审 → 任务必等人、不智能」。洞察**副作用来自工具不来自提示词**：`manual`(playbook 无副作用) 过自动 eval(`skillgen._auto_eval` 沙箱试用+judge≥0.6) → **自动 published(community)、无人卡、`compose_agent` 同轮拼装可用**；`tool`/凭证/危险 → 保留人审墙。留审计痕(approved/decided_by NULL/auto_eval 分)。trust 区分来源：人审 verified、机审 community（均 published 可用）。实测 live：playbook judge 0.98 自动发布。测试 4 例（自动发布/人审墙/同轮拼装/仅本 org 发布）。
  - **B3 记忆自动晋升（飞轮）** ✅：任务成功完成（finalize_run, overall=done）→ best-effort 读本任务产出(result_envelope) → LLM **蒸馏**成若干条可复用客观事实(`center.distill_facts`, JSON+confidence) → 高置信(≥0.7)的 `upsert_shared_fact` 到 **org 记忆**(ns=`company`, `promoted_from`=task_id 溯源)。无人 curate（数据操作、风险低）；org 门更高+必带出处防幻觉沉淀；幂等（同任务已晋升则跳过）。迁移 `d5e6f7a8b9c0` 加 `memory.promoted_from`+`last_promoted_at`。测试 `test_integration_memory_promote.py`(晋升/置信门/溯源/幂等/org 隔离)。**实测 live**：采购分析任务完成 → 蒸馏出 5 条公司知识（「6天交付硬约束」「B公司交期7天被否」「A公司综合成本最低」…）入 org 记忆。
    - ⚠ 实现把 task→role→org 简化为「task 产出 → 蒸馏 → org」（role 层仍由现有 agent_runtime 写回填充）；role 中间层 + 频次门 + task 短时 fact 为后续细化（设计内简化，非疏漏）。改了 finalize_run → **已重启 worker**；dev 库已 `alembic upgrade head`。
  - **B2 分层接地** ✅：规划器出图（生成路径）前检索 **org 记忆**作先验（`service._retrieve_org_memory` 用 goal 向量检索 scopes=['org']，best-effort），注入 `generate_dag` 的 prompt（`_build_user` 加「公司已知（先验，供约束/取舍）」段）。消费 B3 沉淀的公司知识 → 飞轮闭环。k_mem=5（预算雏形，B4 做完整 token 预算）。Agent 侧 role+org 接地 V1 已有。测试 `test_plan_generator`（_build_user 注入/空段）。**实测 live**：B3 沉淀的 5 条供应商事实被 B2 按 goal 向量检索到（「A公司综合成本最低」「6天交付硬约束」「B公司被否」…）作下次规划先验。
    - 简化：B2 只接地「生成路径」；模板命中路径不注入（模板已固定）；A1 模板检索的记忆重排留后续。
  - **B4 预算治理（分层可配置 + 智能缺省）** ✅：回应「长输出别被一刀切掐」。**澄清**：预算管的是**输入上下文**（截输入），输出另有独立上限（仅设上限、绝不截已生成内容）。三层覆盖 **节点>任务>按节点类型智能缺省**：`planner/budget.py`（report/generation/synthesis 等长文本能力 → ctx 8000/out 6000；普通 → 全局缺省 4000/2500，config 可调）。`apply_budgets(dag)` 出图时把解析值回填进每节点，执行期 `agent_runtime`→`run_loop` 直接读：`ctx_budget` 截输入（input_hint>依赖摘要>记忆 优先级保留），`max_output_tokens` 透传到 `gateway.chat`（litellm acompletion max_tokens）。`PlanNode`/`PlanDag` 加可选预算字段（节点级/任务级显式覆盖）。
    - 测试 `test_plan_budget.py`(类型缺省/任务覆盖/节点覆盖/apply 回填) + `test_agent_loop`(run_loop 截输入+传 max_tokens)。**实测 M7 --full 成本门**：4 指标全过、预算内 100%，均成本 ¥0.018→**¥0.0057**（输出上限抑制膨胀），report 节点仍出完整报告(judge 0.9 pass)——控成本不伤长输出。
    - 简化：read_node_output 仍用 FULL_CHARS 字符上限（未改 B_fetch token 化）；k_mem/k_task 精确分项限额走整体 ctx 截断兜底。
  - **🎉 记忆线 B1–B4 全收口**（黑板/晋升/接地/预算）。
  - **V2 阶段门量化（A 内核 + 记忆 收口，2026-06-27）** ✅：跑齐 harness 数据驱动验收——
    - **V1 四指标**（M7 --full 4 目标，全程 A1–A4+B1–B4+分级放行 enabled）：DAG 可用率 100% / 路由命中 100% / 人审通过 100% / 均成本 ¥0.0062·均时延 123s·预算内 100% ✅
    - **A 内核门**（doc 00 §5）：无模板 DAG 可用率 100%(≥70%) / 生成结构合法率(含1次自修复) 90%(≥80%) / 编配覆盖率 100%(≥90%，M7 route 4/4) ✅
    - **检索/记忆专项**：A1 检索命中率 12/12=100%（飞轮省稳）；B3 任务完成蒸馏 5 条入 org；B2 org 先验被检索注入规划；B4 成本随输出上限下降(¥0.018→¥0.006)、长报告不伤 ✅
    - **观察**：M7 --full 4 目标中 3 条终态 needs_review（S1 质量门偏严，把关在收紧）——非门失败（人审通过率仍达标），但「质量门严 ↔ 人审量」的平衡值得在 S2 分级纠错时一并调。
    - **未达门（诚实）**：**B 稳态门**（自动纠错≥60% / 并发无串扰 / 长任务恢复）属协同线 **S2/S3/S4，尚未实现** → 待 S 线后量化。
  - **S2 分级纠错（rework + escalate）** ✅：`workflow._exec_node` 升级——关键节点质量门不达标 → ② **反馈重跑同节点**（把 judge/验收标准喂回 input_hint 重跑，上限 `REWORK_MAX=1`，§4.2）→ 仍不达标 → ④ **escalate**（`escalate_node` Activity 建 `rework` 审批进收件箱，超界交人）。`_assess`/`_run_node` 抽出；测试钩子 `rework_recover`（首次失败返工恢复）。worker 注册 escalate_node。测试：`test_workflow::test_s2_rework_recovers`（返工恢复→done）+ 既有质量门（escalate 路径，stub 跳过建审批）+ `test_integration_escalate`（真建 rework 审批）。**修了一个测试基建坑**：escalate_node 内部 init_engine 建全局引擎绑 loop，测试用完须 `dispose_engine` 否则污染后续（"different loop"）。全量 126 passed、mypy 72/0。
    - **S2b local replan（一刀）** ✅：补 `_affected_subgraph_ids` + `_local_replan`，失败节点可携带替换子图，workflow 圈出失败节点+下游闭包→替换拼接→`validate()`→只重置受影响节点，已完成上游不重跑；无替换子图仍走既有 `_bounded_replan` 兜底。测试 `test_s2b_local_replan_replaces_failed_subgraph`。**LLM 子图生成内核已补**：`generator.generate_subdag` 复用 A2 的结构/语义双校验和自修复，输出可喂给 `_local_replan` 的 replacement nodes。**在线调用已接线**：`generate_replan_subdag` Activity 承载 LLM 调用，显式 `local_replan=true` 可 opt-in；worker 已注册 Activity，测试 `test_s2b_local_replan_can_call_generation_activity`。**自动策略已补**：非 human/system、非 dangerous 节点失败且会牵连下游闭包时自动调用在线局部重规划，`local_replan=false` 可禁用；测试 `test_s2b_local_replan_auto_for_failed_subgraph`。**边界回归已补**：叶子失败、显式禁用、system、dangerous 节点均不调用在线子图生成，继续走保守 bounded replan。**稳定性回归 gate 已补**：`scripts/s2_s3/stability_regression_gate.py` 串起 `test_workflow.py` + `test_plan_generator.py` + S3 队列集成测试，完整本地 PASS。剩余：更大业务样本。
  - **S3 并发闸 + 预算提示** ✅：`_start_plan` 在连 Temporal 前先过两道（§6）——① **并发真实闸**：org 在跑 task_run 数达 `org_max_concurrent_runs`(默认5) → 保公平；② **预算只提示**：累计预估成本(`repo.org_estimated_cost_cents`)达 `org_budget_cents`(默认0=关) → 记 warning，**照常执行不阻断**。repo 加 `count_active_runs`/`org_estimated_cost_cents`。测试 `test_integration_concurrency` 覆盖灌满 limit 与聚合校验。
    - **S3 公平队列** ✅：并发满时不再 429，登记 `task_run(status='pending')` 入队并返回 `pending`；`GET /plans/{id}/run` 对 pending 直接返回，不连 Temporal。`finalize_run` 终态释放槽后 best-effort 自动出队并落 Run Manifest。**多槽优先级出队已补**：按 `org_max_concurrent_runs - running` 一次填满空槽；pending 排序为显式 `task_run.priority DESC` → 短作业优先（`plan.estimated_cost_cents ASC`）→ FIFO；迁移 `f1a2b3c4d5e6` 加 `task_run.priority` 默认 0，迁移 `0a1b2c3d4e5f` 加 `task.priority` 默认 0；任务级 priority 来源已接入（`task.priority` → run priority，创建/出图/运行/列表响应贯通）；**SLA/任务类型自动映射已补**：未显式传 `priority` 时从 `inputs.sla` 与 `inputs.task_type`/`taskType`/`type` 派生优先级，显式 `priority`（含 0）优先；测试覆盖默认短作业优先、高 priority 覆盖低成本、同 priority/同成本 FIFO、120 条 pending 批量排序压力样本，以及 SLA 派生/显式覆盖。已纳入 `scripts/s2_s3/stability_regression_gate.py` 完整 gate。剩余：真实流量/更大业务样本复测。
  - **S4 长时运行** ✅：`run_node` 节点活动**后台周期 heartbeat**（每 10s）+ `heartbeat_timeout=40s` → worker 崩溃被 Temporal 快速发现重试（不死等 5min start_to_close）；human gate = durable wait（V1 已有）；Temporal 持久（天然）。**ADR-0012 accepted**（协同框架 S1–S4 决策定稿）。全量 127 passed、mypy 72/0。
    - 简化（S4 剩余）：**workflow versioning(patching) 作为纪律确立**——自下次改编排代码起用 `workflow.patched()` 守新分支（本次未对既有分支追加 patch：已部署、无在飞任务受影响）；heartbeat 为后台 ticker（非按 agent-loop 步进度），够用。
  - **🎉 协同线 S1–S4 落地**（状态机质量门/分级纠错/并发预算/长跑）。**B 稳态门**后续增强：S2b/S3 更大业务样本复测。
  - **B 稳态门量化 + 回炉（用数据不堆模块）** ✅：跑 M7 --full（S1–S4 active）量化 B 门——**自动纠错率~67%(≥60)✅**（S2 rework 把任务完成率从 25%→75%）、**并发无串扰✅**（隔离回归 2 passed）、**长任务恢复**=Temporal 持久+S4 heartbeat（机制）。任务完成率初测 75%<90%，诊断发现**唯一顽固目标其实是方差**（同目标再跑 judge=1.0/done），根因＝**质量门阈值漂移**：evaluate_node 用 evaluator 通用默认 0.7，但设计 τ_pass≈0.6（§4.3/§6/ADR-0012）→ 好产出因 judge 方差跌破 0.7 被误判。**回炉（不堆模块）**：`config.quality_gate_tau=0.6` + `evaluate_node` 用它（`judge≥τ ∧ 断言过`），对齐设计、降假阴性；**没有**建 S2③ local replan / S3 队列（数据不支持为清门去堆）。全量 127 passed、mypy 72/0。
    - **judge 方差才是真瓶颈**（回炉根因）：B 门量化发现「顽固目标」其实是 LLM judge 噪声（同报告 0.5↔1.0 乱跳→随机误判 needs_review）。落 design §6.1 防糊弄——`evaluator._llm_judge` 加**四维度(切题/完整/可执行/有据)+0~1 分段锚点**。实测同报告判 5 次：好报告 [0.92~0.98] 极差 **0.06**（原 ±0.5）、差报告 mean 0.18，稳定且区分清晰。
    - **🎉 任务完成率 25%→75%→100%**（M7 --full 6 目标，τ=0.6 + 稳定 judge）：**6/6 done、judge 0.75~0.97、人审 100%、DAG/路由 100%、均成本 ¥0.0064、预算内 100%**。**B 稳态门用回炉清掉、未堆 S2③/S3 模块**（自动纠错 rework≥60% + 任务完成 100% + 并发无串扰隔离✅ + 长任务恢复=Temporal持久/S4 heartbeat 机制）。
    - 剩余（非清门必需、按需做）：S2b/S3 更大样本复测、双 judge/回归集进一步降噪。
  - **下一步**：做 B 稳态更大业务样本复测，或用真实用户任务继续观察 R4 自然复用率。
- **产品线 P2/P3/P4** ✅（2026-07-02~03）：
  - **P2 附件 + MinIO** ✅：MinIO 容器 + ObjectStore 客户端 + `POST /api/tasks/{id}/attachments` 上传 + attachment 登记 + 运行前必填附件校验
  - **P3 执行记录 + 导出** ✅：任务页展开历次 run + `POST /api/plans/{id}/export`（md/pdf）+ 下载 blob + artifact 留存
  - **P4 看板** ✅：`GET /api/dashboard`（总运行/成功率/平均耗时/进行中/复用命中率/人审通过率/成本/token）+ 状态分布 + 场景分布 + 预算提示；前端 `/orgs/{id}/dashboard` 页
  - **导航扩「看板」**：AppShell 左侧菜单加看板入口
- **R3 场景模板沉淀** ✅（2026-07-03）：
  - PlanTemplate 扩 `domain`/`subcategory`/`acceptance_criteria`/`source` 字段（+ migration）
  - `POST /api/plans/{id}/save-as-template` — 幂等存为私有模板（同名覆盖+bump version）
  - `GET /api/catalog/templates` — 场景库货架 API（可选 `?domain=` 筛选）
  - 前端工作详情页「💾 存为模板」按钮
  - 计划模板查询加可见性过滤（R1/R3 底座：`visible_clause` 防跨租户泄露）
  - **R3 加固** ✅：存为模板时 best-effort 即时写 `PlanTemplate.embedding`（TEI 不可用则降级后续 backfill，不阻断保存）；
    回归覆盖幂等覆盖+bump version、domain 筛选、私有模板跨 org 不可见、embedding 非空。
- **P5 场景库树导航** ✅：
  - AppShell 左侧新增「场景库」入口
  - `/orgs/{id}/scenarios` 按 `domain > subcategory > template` 树形浏览可见模板
  - 场景卡展示来源/可见性/版本，并可用模板名称作为目标跳转出图
- **R4 生成 Agent → role_template 自动沉淀** ✅（2026-07-03，机制完成并验证）：
  - 新增 `role_template` 资产表（`visibility/source/status/owner_org_id/skill_refs/capabilities/persona/embedding/meta` + hnsw）
  - `compose_agent` 在 generated Agent 通过试产出 eval 后，自动 upsert 私有 `role_template(source=generated)`，记录 `agent_id`、版本、provenance、eval 快照与 skill_refs
  - 已有 generated Agent 幂等复用时也会补沉淀；`embed_backfill` 纳入 role_template 空 embedding 回填
  - **R4 复用率量化底座** ✅：Run Manifest `agents_used` 口径改为 node→Agent 名称（不再写能力列表），新增 `scripts/r4/role_template_reuse_gate.py`，按 generated role_template 对应 Agent 在 manifest 中出现次数量化复用率。默认排除 `meta.sample=true` 的最小机制样本；用法：`uv run python scripts/r4/role_template_reuse_gate.py --threshold 0.6 [--org-id <org>]`
  - **R4 gate 最小 live 样本闭环** ✅（2026-07-04）：新增 `scripts/r4/seed_reuse_sample.py`（默认 dry-run，`--write` 显式写入），可创建最小 generated role_template + 2 条 manifest 复用样本。已跑：`uv run python scripts/r4/seed_reuse_sample.py --write` → `uv run python scripts/r4/role_template_reuse_gate.py --threshold 0.6 --include-samples`，结果 `1/1 = 100%`、✅ PASS。默认真实业务口径仍为 `NO DATA`（排除 sample），后续需真实业务样本验证自然复用率。
  - **R4 自然复用集成证据** ✅：`tests/test_integration_composer.py::test_r4_reuse_gate_counts_natural_generated_role_template` 走真实 `compose_agent` 沉淀 role_template，再落两条 Run Manifest，验证默认 gate 输入（排除 sample）可计算并通过。注意：这是 testcontainers 临时库集成证据；dev/live 默认口径仍需真实业务运行样本。
  - **R4 dev/live 自然编配 smoke** ✅：新增 `scripts/r4/seed_natural_reuse_smoke.py`（默认 dry-run，`--write` 显式写入），会创建/复用专用 org + published manual Skill，经 `compose_agent` 自然沉淀 generated role_template，再补足 manifest 出现次数。已跑：`uv run python scripts/r4/seed_natural_reuse_smoke.py --write` → 默认 `uv run python scripts/r4/role_template_reuse_gate.py --threshold 0.6`，结果 `1/1 = 100%`、✅ PASS（`弹性组队·r4.reuse.natural` occurrences=2）。2026-07-06 复跑默认 gate 仍 PASS：`1/1 = 100%`。
  - 验证：ruff/mypy/compileall 通过；Alembic revision 链为 `a384baa396e7 -> e6f7a8b9c0d1`；dev DB `alembic upgrade head` 通过；Docker testcontainers 集成测试 `tests/test_integration_composer.py tests/test_integration_templates.py` 通过
- **本批次约束遵循记录（docs/constraints）**：
  - F1/F2：R3/P5/R4 均已映射到 V2 WBS；任务清单和续接指南已同步，未留未登记 TODO/skip
  - E9/C6：新增表只走 Alembic，迁移可 downgrade，已在 dev DB 与 testcontainers 临时库验证
  - E10/E11/C14/C15：`ruff check .`、`mypy src`、全量 `pytest -q -p no:warnings`、R3/R4 targeted 集成测试通过
  - E14/C16：`bandit -r src`、`gitleaks protect --redact`、`pip-audit` 通过（本地包 `polis` 非 PyPI，正常跳过）
  - 前端 B9/B12：`pnpm exec tsc --noEmit`、`pnpm build` 通过；当前项目尚无 eslint/prettier 脚本，沿用既有前端门禁
  - 评审注意：前端约束文档要求 React Query/OpenAPI 生成类型，但当前代码基线使用 `frontend/lib/api.ts` 手写 client；本批次按既有项目模式扩展，后续若统一数据层需单独开任务
- **C0 交互重构一期** ✅（2026-07-01，纯前端 IA 重排 + 配套 BE）：
  - **C0-1** ✅ 应用壳 + 工作台首屏（`28549ca`）
  - **C0-2** ✅ 工作列表进 AppShell → 对照原型重做为 tabs 筛选 + 表格式布局（全部/进行中/待处理/已完成）
  - **C0-3** ✅ 工作详情进 AppShell → 对照原型重做为三 tab 导航（计划与运行/完整日志/用量与成本）
  - **C0-4** ✅ 工作台「进行中 + 最近产出」双栏卡片（进度条卡 + 独立产出卡 + 统一框图容器）
  - **C0-5** ✅ 花名册视觉精修（Agent 双栏网格 + 彩色头像 + 角色分组）
  - **设置页** ✅ 套入 AppShell（公司信息卡 + 模型配置卡并排 + 删除公司危险区）
  - **后端配套**：`GET /api/runs/workspace`、`GET /api/plans/{id}`、`TaskRunOut`/`WorkspaceRunItem` 扩 actual_cost + node_count
  - **样式系统**：全宽铺满、DAG SVG 270×128、工作列表表格列宽 2.4:1:1:0.7:0.8:1.3、按钮收口（「＋ 新建公司」仅工作台顶栏）
  - **hero 背景图** ✅：`task-bg.png` **重画为全出血横幅**（2000×400，numpy+Pillow 生成：对角靛蓝渐变 `(60,58,198)→(82,80,224)`
    + 底部上升波浪亮部 `(150,142,246)` + 右上点阵，**图内不烤圆角/白边**）。`.wb-hero` 固定 `height:240px` + `background-size:cover`，
    **圆角由 CSS `border-radius:16px + overflow:hidden` 出** → 任意宽度稳定 240px、圆角干净、不变形。内容 `padding:28px 32px`。
    （迭代收敛：cover+渐变=色差/圆角裁 → 整幅原图 514px 太高 → 裁白边 3.2:1 仍 368px 偏高 → **重画全出血图，固定 240px**。
    原始设计图备份见会话 scratchpad `task-bg-original.png`。）C0 一期无剩项。
  - **C0 二期收尾** ✅（2026-07-03）：
    - **场景库分类管理** ✅：`scene_category` 表 + CRUD（domain/subcategory）+ 场景树内联增删改（＋✎−）+ 双击编辑 + 删除模态框级联提示
    - **工作台整合看板** ✅：Hero 下方数据看板（8 项统计 + 状态分布 + 场景分布），AppShell 移除独立看板导航
    - **任务流程修复** ✅：`POST /api/tasks/{id}/plan` 出图+pending run → approve 时自动贯通 task_id → 状态实时更新
    - **再次运行不复出图** ✅：复用已有 plan_id 调 approvePlan，不重新生成 DAG
    - **任务删除** ✅：`DELETE /api/tasks/{id}` 级联 + 前端模态框确认
    - **数据修正** ✅：dashboard avg_duration envelope 兜底、工作列表成本改实际费用
- **阶段 C 产品化（2026-07-02）** ✅ T7.6 / P2b / P3 / P4 全部完成（详见 [研发任务清单](plan/研发任务清单.md) 对应任务卡）：
  - **T7.6 MinIO 对象存储** ✅：compose 服务(9000/9001) + `modules/storage`（put/get/presigned/delete，`{org}/{task}/{name}` 前缀隔离）。
  - **P2b 附件消费** ✅：任务附件上传/列表/下载/删除 API + 运行时黑板注入（清单默认注入 + `read_attachment` 懒加载正文）+
    `input_schema` 必填附件校验（422）。**附件从上传到被 Agent 读取影响产出，端到端打通**。
  - **P3 执行记录 + 导出** ✅：`POST /api/plans/{id}/export?fmt=md|pdf`（reportlab 内嵌真实中文字体渲染，非 CID 引用式，
    避免空白 PDF）+ 工作列表「执行记录」弹窗（全部历次运行 + 查看观测/导出，补回 C0-2 重排后只显示最新一次的缺口）。
  - **P4 看板** ✅：`GET /api/dashboard`（org 级跨任务聚合：状态分布/成功率/平均耗时/复用命中率/人审通过率/
    按场景分布/进行中数/预算提示/近期实测成本token，全部复用 S3/观测已有基建，无新表）+ `/orgs/[id]/dashboard` 新页
    （AppShell 加「看板」导航）。实测 live：5 运行+3 approval 场景下各聚合数值精确正确。
  - ⚠ 环境提醒：`frontend/.env.local` 的 `NEXT_PUBLIC_API_BASE` 若被设成局域网 IP 会不稳定（IP 会变），本机开发建议
    `http://localhost:8000`（改 `NEXT_PUBLIC_*` 需重启前端 dev server，非热更新）。
  - 下一步：场景库飞轮(R3/R4/P5)。

> ✅ DeepSeek 余额已充（曾 `Insufficient Balance`，现真实 LLM 运行正常：A2 验证门 10 目标全过、任务跑出真实产出）。默认模型现为 **flash**（见上「模型坑」）。
> ⚠ **改了 worker 代码后需重启 `make worker`**；**改了 `.env` 后需重启后端**（uvicorn --reload 只重载 .py，不重载 .env）。

> **真实服务起栈**（M6 后）：postgres/temporal/`text-embeddings`(TEI,模型预下载 infra/tei-models 挂载)/`langfuse`(3001)
> + 后端 `make dev` + `make worker`；DeepSeek Key 走 backend/.env 或界面 credential 配置。
> 起 Langfuse：`cd infra && docker compose --env-file .env up -d langfuse`。
> ⚠️ **改了 worker 代码必须重启 `make worker`**（无 --reload）；agent config 未配 model 时回退 default_chat_model。
>
> **M6 仍待（非阻塞）**：Guardrails→Guardrails-AI、MCP→真实外部 server、多模型/主模型/按 Agent 选模型(第二步)、
> LiteLLM reranker/语义去重、Langfuse trace_ref 落库（当前直查 langfuse API by task_id，未用 trace_ref 表）。

> 当前 git：`main` 含 M0–M6（含 Langfuse 自建观测页 + M6-H-4 观测页 UX 重构：SVG DAG/markdown 折叠卡/token 成本面板）。
> 远程 `https://github.com/multifort/Polis`（私有）。提交身份已设 `multifort <fkdtz2008@gmail.com>`。

## 3. 怎么把环境跑起来
```bash
# 基础设施（postgres+pgvector 已常驻；temporal/litellm/langfuse 按需）
cd /Users/lining/workspace/polis/infra && cp -n .env.example .env && docker compose up -d postgres
# 后端（务必用 make dev，带 --reload；手动 nohup 起会拿旧代码）
cd ../backend && make dev          # http://localhost:8000 ，文档 /docs
# 前端
cd ../frontend && pnpm install && pnpm dev   # 默认 3000（被占则换端口）
# 灌种子（能力词表/模型目录/预设/计划模板，幂等）
cd ../backend && make seed
# 对象存储（V2-P2a：附件/结果产物）—— 起 MinIO（infra/.env 需 MINIO_ROOT_USER/PASSWORD）
cd ../infra && docker compose --env-file .env up -d minio   # 9000 S3 API / 9001 控制台
#   backend 用同一对凭证：POLIS_MINIO_ENDPOINT=localhost:9000 / ACCESS_KEY / SECRET_KEY（见 backend/.env.example）
# 编排（M3：跑计划运行需要）—— 起 Temporal + Polis worker
cd ../infra && docker compose --env-file .env up -d temporal temporal-ui  # 7233 server / 8233 UI
cd ../backend && make worker       # 连 7233，监听 polis-tasks 队列（前台进程）
```
- Temporal 未起时 `POST /approve`、`GET /run`、`POST /signal` 优雅降级返回 **503**（前端有「编排服务未就绪」提示）。
- DB：`localhost:5432` user=`polis` pwd=`polis_local_dev` db=`polis`（`backend/.env` 的 `POLIS_DATABASE_URL`）。
- 测试账号：`demo_li@polis.dev` / `secret123`（没有就注册 + 用预设"采购分析公司"创建一家公司）。

## 4. 技术栈与关键架构
- 后端：Python 3.12 + FastAPI + SQLAlchemy 2.0(async) + Alembic + Pydantic v2，`uv` 管理。前端：Next.js 14 App Router + TS + pnpm。
- 分层（照 `modules/org` 写）：`schemas.py`(Pydantic) / `repository.py`(集中 SQL) / `service.py`(业务，抛领域异常) / `api.py`(路由，异常→HTTP)。模块在 `backend/src/polis/modules/{org,planner,runtime,memory,model,observability}`。
- **多租户**：组织级端点用依赖 `from polis.modules.org.deps import CurrentOrg`（校验 `X-Org-Id` 成员 + 每请求 `SET LOCAL ROLE polis_app` + `set_config('app.current_org',…)` → RLS 生效）。`CurrentUserId` 取当前用户。`get_session` 是**每请求一个事务**，service 只 `flush` 不 commit。RLS 细节见 `docs/design/0b §3`、`docs/decisions/0005`。
- 数据模型权威地图：`docs/design/0b`（27 表 + 隔离分层 + 扩展策略）。迁移只走 Alembic、向后兼容。
- 测试：`backend/tests/`，`conftest.py` 的 `pg_url`(testcontainers 起临时 pgvector + 跑 alembic) 与 `client`(TestClient) fixture。门禁三件套：`uv run ruff check .` / `uv run mypy src` / `uv run pytest -q -p no:warnings`，全绿才提交。
- 提交：Conventional Commits，结尾 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。

## 5. 关键决策（ADR）
- 0001 与 hermes-test 完全独立；0002 后端 Python/FastAPI；0003 命名 Polis（用户向"虚拟公司"，不出现"立邦/城邦"）；0004 复用优先栈；0005 多租户=逻辑隔离+RLS兜底(SET ROLE polis_app)；
- **0006（最重要的路线决策）**：M2/M3 走**确定性「模板优先」**，**LLM/embedding 相关延后到 M6**（模型网关 LiteLLM）。M3 的 Temporal 编排**在 M3 做**，但节点执行用**确定性桩**，真实 Agent+LLM 执行留 M4。

## 6. ⚠ 环境踩坑（避免重复踩）
1. **会话开在 `/Users/lining/workspace/polis`**。若开在别的项目根（如某 hermes 仓库），cwd/git 会串台。
2. **后台子代理（run_in_background / 无人值守）没有 Bash/Read 权限**，且 worktree 会基于会话根仓库而非 Polis → 跑不通。要委派就用**前台子代理**（一次一个、会阻塞、但有 Bash），或直接自己做。
3. Bash 工具：涉及网络/docker/uv/pytest起容器/git push 的命令要传 `dangerouslyDisableSandbox: true`；shell 有 `set -e`，用 `grep` 前加 `set +e;`，复杂命令易吞输出，必要时写 `/tmp` 再 Read。
4. **pre-commit 钩子**会用 ruff-format 改文件导致首次 `git commit` 中止——遇到就 `git add -A` 再 commit 一次。
5. **后端改了代码要用 `make dev`（带 --reload）**；手动 `nohup uvicorn …`（无 --reload）会一直跑旧代码，调试会被坑（已踩两次）。
6. mypy strict：`session.scalar(...)` 返回 Any → 先赋给带类型标注的变量再 return；ORM 的 `Mapped[dict]` 要写 `Mapped[dict[str, Any]]`；第三方缺 stub 在 `pyproject.toml` 加 `[[tool.mypy.overrides]] module=["x.*"] ignore_missing_imports=true`。
7. pgvector 用 pg18 镜像（华为云源拉取后 retag）；pg18 卷挂 `/var/lib/postgresql`（非 `/data`）。

## 7. 下一步

> M0–M7 + V2 阶段 0/A/B + C0 交互重构一期 + P2/P3/P4 + R3/P5/R4 机制均已完成。后端能力（出图/任务/运行/观测/审批/花名册/用量/记忆/对象存储/附件/导出/看板/存为模板/角色模板沉淀）就绪，前端按新 IA 六屏（工作台/工作列表/工作详情/场景库/看板/花名册）已可走通主路径。

后续优先级：
1. 继续观察真实业务样本 R4 gate 自然复用率 ≥60%：`uv run python scripts/r4/role_template_reuse_gate.py --threshold 0.6`
   - 默认排除 `meta.sample=true` 最小样本；dev/live 自然编配 smoke 可用 `uv run python scripts/r4/seed_natural_reuse_smoke.py --write` 打通（当前默认 gate PASS：1/1=100%，2026-07-06 复跑仍 PASS）。
   - 若只验证机制闭环，可先用 `uv run python scripts/r4/seed_reuse_sample.py --write`，再跑 `uv run python scripts/r4/role_template_reuse_gate.py --threshold 0.6 --include-samples`（当前最小样本 PASS：1/1=100%）。
2. 复跑 S2b/S3 稳定性回归 gate：`uv run python scripts/s2_s3/stability_regression_gate.py --keep-going`
   - 无 Docker/testcontainers 时可先用 `--skip-docker` 跑 S2b 子集；当前完整本地 gate PASS（S2b workflow 11/11、子图生成 10/10、S3 队列 6/6）。
3. 更大业务样本复测 B 稳态门（dry：24 目标 DAG 95.8% / 路由 97.8%；full 小样本 2/2 done；后续按成本窗口扩到 6/12/24；M7 harness 已补 B 任务完成率、`needs_review` 终态识别、`--goals-file` 大样本入口）
4. 按需扩展 SLA/任务类型字典（当前已支持 `inputs.sla` 与 `inputs.task_type`/`taskType`/`type` 的保守映射）

## 8. 已知技术债（M3 + 技术债清理批次后）
- 见 `docs/tech-debt.md`（2026-06-20 清理批次后已更新台账）。
- **已 closed**：TD-004(镜像固定)/TD-005(工具锁定)/TD-006/007/010/TD-012(认证增强:登出/轮换/清理)/
  TD-015(org_scoped 助手)/TD-018(沙箱告警)。
- **部分偿还（剩余项明确）**：
  - **TD-011** 认证/审批成功事件已审计；剩**登录失败审计**（随限流做）。
  - **TD-013** 生产 fail-closed 校验(JWT/CORS)已落地；剩**登录限流 + 找回密码**（对外前）。
  - **TD-014** 前端静默刷新已落地；剩 **localStorage→httpOnly cookie** 硬化。
  - **TD-016** approve/signal 已加 owner/approver 守卫；剩**成员邀请/移除 + 前端角色门控**。
- **仍 open（设计内后置/依赖未来）**：TD-001/002/003/009（分支保护/CI/容器化）、
  TD-017(中文语义匹配→M6)、TD-019(终态 workflow 回调→M6)、**TD-020(Planner 全自动 LLM 拆解兜底→M6)**。
