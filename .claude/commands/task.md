---
description: 按就绪定义(DoR)建一条研发任务
---
为「$ARGUMENTS」创建一条符合 DoR 的研发任务条目，追加到 `docs/plan/研发任务清单.md` 对应里程碑下：

字段：任务ID（沿用 T<模块>.<序号> 体系）/ 标题 / 关联设计章节 / 依赖任务 / 交付物 / 验收标准 / 估时(1~3天否则拆分)。
DoR 自检（全部满足才算就绪）：
```
□ 目标/验收标准明确  □ 关联设计章节  □ 依赖已就绪  □ 无重大未决问题  □ 1~3 天内可完成
```
不满足就标注缺口、先别置为「就绪」。
